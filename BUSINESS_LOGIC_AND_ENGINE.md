# ⚙️ سند جامع منطق محاسباتی، فرمول‌های ریاضی و موتور هسته (Bushido OS - Business Logic & Engine Specification)
> **نسخه:** ۲.۰ (Principal Architect & Core Engine Edition)  
> **سامانه:** سیستم عامل دیسیپلین فردی بوشیدو (Bushido Discipline OS)  
> **مرجعیت:** Single Source of Truth برای کلیه محاسبات ریاضی، امتیازدهی، مخرج شبح، الگوریتم زنجیره و موتور رفتاری  

---

## ۱. ارکان فونداسیون و فرمول ارزش روزانه (Daily Score Model)

هر روز نبرد در سیستم بوشیدو از **۵ رکن بنیادین فونداسیون (Foundation Habits)** و **۱ ماموریت شتاب‌دهنده ویژه (Special Mission)** تشکیل شده است:

```text
+-------------------+---------------------+--------------------+--------------------+
| کلید فونداسیون    | عنوان فارسی         | وزن پایه (Base Pt) | شرط روز استاندارد |
+-------------------+---------------------+--------------------+--------------------+
| wakeUp            | سحرخیزی            | ۱ امتیاز           | الزامی (۱ از ۵)   |
| workout           | ورزش و تحرک         | ۱ امتیاز           | الزامی (۲ از ۵)   |
| study             | مطالعه تخصصی        | ۱ امتیاز           | الزامی (۳ از ۵)   |
| journal           | ژورنال‌نویسی         | ۱ امتیاز           | الزامی (۴ از ۵)   |
| hardTask          | کار سخت روز         | ۱ امتیاز           | الزامی (۵ از ۵)   |
+-------------------+---------------------+--------------------+--------------------+
| specialMission    | ماموریت ویژه روزانه | ۲ امتیاز پاداش     | اختیاری (شتاب‌دهنده)|
+-------------------+---------------------+--------------------+--------------------+
```

### فرمول ریاضی محاسبه ارزش روز ($S$):

$$H = \sum_{i=1}^{5} \mathbb{I}(\text{habit}_i = \text{true}) \quad \in \{0, 1, 2, 3, 4, 5\}$$

$$\text{isStandard} = \begin{cases} \text{true} & \text{if } H = 5 \\ \text{false} & \text{if } H < 5 \end{cases}$$

$$\text{Bonus} = \begin{cases} 3 & \text{if isStandard is true} \\ 0 & \text{if isStandard is false} \end{cases}$$

$$\text{MissionScore} = \begin{cases} 2 & \text{if specialMission is true} \\ 0 & \text{if specialMission is false} \end{cases}$$

$$\text{Score} = \min(10, H + \text{Bonus} + \text{MissionScore})$$

### جدول حالات ممکن امتیاز روز:
- **۰ تا ۴ پایه بدون ماموریت:** امتیاز = $0 \text{ to } 4$ (روز سوخته)
- **۰ تا ۴ پایه با ماموریت:** امتیاز = $2 \text{ to } 6$ (روز سوخته - ماموریت ویژه بدون تکمیل ۵ پایه نمی‌تواند روز را استاندارد کند)
- **۵ پایه بدون ماموریت:** امتیاز = $5 + 3 = 8$ (روز استاندارد ۵/۵ 🟢)
- **۵ پایه با ماموریت ویژه:** امتیاز = $5 + 3 + 2 = 10$ (کمال تعهد ۱۰/۱۰ 🏆)

---

## ۲. طبقه‌بندی وضعیت روزها (Day Status Taxonomy)

وضعیت هر روز ($DayStatusType$) بر اساس قواعد زیر تعیین می‌شود:

```text
+-----------------------+--------------------------------------------------------------+--------------------+
| وضعیت روز             | شرایط و معیارها                                             | نشانگر بصری        |
+-----------------------+--------------------------------------------------------------+--------------------+
| standard              | H = 5 (هر ۵ پایه اجرا شده باشد)                              | 🟢 روز استاندارد   |
+-----------------------+--------------------------------------------------------------+--------------------+
| personal_frozen       | H < 5 و دلیل افت = «دلایل شخصی» (توقف موجه اضطراری)           | ❄️ فریز شخصی       |
+-----------------------+--------------------------------------------------------------+--------------------+
| burned_resolved       | H < 5 و کاربر دلیل افت و زمان آن را در کالبدشکافی ثبت کرده است | 🟣 کالبدشکافی شده  |
+-----------------------+--------------------------------------------------------------+--------------------+
| burned_unresolved     | H < 5 و کاربر هنوز کالبدشکافی را تکمیل نکرده است             | 🔴 بدهی باز معوقه  |
+-----------------------+--------------------------------------------------------------+--------------------+
```

---

## ۳. شاخص دیسیپلین و فرمول مخرج شبح (Phantom Denominator Formula)

برخلاف سیستم‌های سطحی که میانگین ساده می‌گیرند، سیستم بوشیدو از **الگوریتم مخرج شبح (Phantom Denominator)** برای جریمه رفتاری اهمال‌کاری استفاده می‌کند:

### گام ۱: شمارش روزهای موثر و ارزیابی‌شده

$$\text{raw\_elapsed} = \begin{cases} \text{elapsedDays} - 1 + \mathbb{I}(\text{isTodayCompleted}) & \text{if cycle is active} \\ \text{elapsedDays} & \text{otherwise} \end{cases}$$

$$\text{evaluated\_days} = \max(0, \text{raw\_elapsed} - n_{\text{frozen}})$$

که در آن:
- $n_{\text{frozen}}$: تعداد روزهای فریز شده موجه است (فریز از مخرج کسر می‌شود تا به کاربر ظلم نشود).
- $\text{isTodayCompleted}$: آیا روز جاری به وضعیت استاندارد یا کالبدشکافی رسیده است یا خیر.

### گام ۲: شمارش دسته‌های ۵‌گانه لاگ‌ها

1. **$n_1$:** تعداد روزهای استاندارد ($H=5$).
2. **$n_2$:** تعداد روزهای کالبدشکافی‌شده با تلاش نسبی ($H \in \{1, 2, 3, 4\}$ همراه با کالبدشکافی).
3. **$n_3$:** تعداد روزهای کالبدشکافی‌شده با انفعال کامل ($H=0$ همراه با کالبدشکافی).
4. **$n_4$:** تعداد روزهای بدهی باز با تلاش نسبی ($H \in \{1, 2, 3, 4\}$ بدون کالبدشکافی).
5. **$n_5$:** تعداد روزهای بدهی باز با انفعال کامل ($H=0$ بدون کالبدشکافی).
6. **$\text{missing\_days}$:** روزهای سپری‌شده‌ای که کاربر اصلاً به برنامه سر نزده و هیچ لاگی ثبت نکرده است:

$$\text{missing\_days} = \max(0, \text{evaluated\_days} - \text{active\_valid\_logs})$$

### گام ۳: محاسبه مخرج شبح ($P$) و شاخص انضباط ($D$)

$$P = \text{evaluated\_days} + (0.2 \times n_3) + (1.5 \times n_4) + (3.0 \times (n_5 + \text{missing\_days}))$$

$$\text{disciplineScore} = \begin{cases} 1.0 & \text{if evaluated\_days} = 0 \\ \max\left(0, \min\left(1, \frac{n_1}{P}\right)\right) & \text{if evaluated\_days} > 0 \text{ and } P > 0 \end{cases}$$

$$\text{disciplinePercentage} = \text{round}(\text{disciplineScore} \times 100)$$

### تحلیل رفتاری ضرایب مخرج شبح:
- **ضریب ۰.۲ ($n_3$):** جریمه سبک برای روزی که کاربر کاری نکرده اما شجاعانه کالبدشکافی کرده است.
- **ضریب ۱.۵ ($n_4$):** جریمه سنگین برای بدهی کالبدشکافی نشده با وجود تلاش نسبی.
- **ضریب ۳.۰ ($n_5 + \text{missing}$):** شدیدترین جریمه برای فرار از نبرد و عدم ثبت علت شکست (سقوط سریع شاخص انضباط).

### سطوح ۴‌گانه شاخص انضباط:
- **$D \ge 80\%$:** 🛡️ انضباط آهنین (Iron Discipline)
- **$60\% \le D < 80\%$:** ⚔️ انضباط پایدار (Stable Discipline)
- **$40\% \le D < 60\%$:** 👺 انضباط ناپایدار (Unstable Discipline)
- **$D < 40\%$:** 👹 بحران تعهد (Commitment Crisis)

---

## ۴. الگوریتم محاسبه زنجیره خالص (Pure Streak Algorithm)

زنجیره خالص (`Pure Streak`) ستون اصلی تاب‌آوری عادت است. الگوریتم پیمایش خطی از ابتدای چرخه تا روز جاری به شرح زیر است:

```text
الگوریتم محاسبه Pure Streak:
--------------------------------------------------------------
ورودی: لیست تمام روزهای چرخه مرتب‌شده بر اساس تاریخ صعودی
خروجی: currentPureStreak و maxPureStreak

۱. runningStreak = 0
۲. maxPureStreak = 0
۳. برای هر روز i از 0 تا N:
    الف) اگر فاصله تقویمی روز i با روز i-1 بیشتر از ۱ روز باشد (شکاف تقویمی):
         runningStreak = 0
    ب) اگر روز i روز استاندارد باشد (isStandard = true):
         runningStreak = runningStreak + 1
         اگر runningStreak > maxPureStreak:
             maxPureStreak = runningStreak
    ج) اگر روز i فریز شخصی باشد (statusType == 'personal_frozen'):
         // روز فریز زنجیره را قطع نمی‌کند اما به عدد آن اضافه نمی‌شود (Pass-through)
         ادامه به روز بعدی
    د) اگر روز i روز جاری نبرد باشد و هنوز دلیل شکستی برای آن ثبت نشده باشد:
         // روز جاری هنوز در جریان است و شکست نخورده؛ زنجیره روزهای قبل حفظ می‌شود
         ادامه به روز بعدی
    هـ) در غیر این صورت (روز سوخته گذشته یا شکست قطعی):
         runningStreak = 0

۴. currentPureStreak = runningStreak
۵. globalLiveStreak = inheritedStreak + currentPureStreak
```

---

## ۵. ماتریس هوشمند سنسی و تله‌های شناختی (Deterministic Sensei Rules)

موتور سنسی هوش مصنوعی (`deterministicSensei.ts`) بدون وابستگی به سرورهای خارجی، ماتریس زیر را برای ریشه‌یابی و ارائه راهکار تاکتیکی اجرا می‌کند:

```text
+---------------+------------------------+------------------------------------+------------------------------------------+
| زمان افت      | انگیزه و دلیل ثبت‌شده  | تله شناختی کشف‌شده                 | قانون مقابله و استراتژی تاکتیکی فردا      |
+---------------+------------------------+------------------------------------+------------------------------------------+
| اول روز       | بی‌برنامه بودم         | تله اینرسی صبحگاهی و فاصله اصطکاک  | قانون ۳۰ دقیقه اول: ورود بدون لمس گوشی   |
| اول روز       | نیمه‌کاره رها کردم     | تله کمال‌گرایی منفی در شروع         | قانون ۵ دقیقه اول: تمرکز فقط روی گام اول |
| وسط روز       | وقتم رو مدیریت نکردم   | تله افت دوپامین پس از ظهر (Slump)  | قانون بلوک عمیق ۹۰ دقیقه‌ای با ۵ دقیقه پاز|
| وسط روز       | نیمه‌کاره رها کردم     | تله اصطکاک شناختی و ابهام تسک      | خرد کردن وظیفه سخت به زیربخش‌های ۲۵ دقیقه‌ای|
| آخر روز       | وقتم رو مدیریت نکردم   | تله تخلیه مخزن اراده و اهمال شبانه | قانون خط قرمز ساعت ۲۱: تیک قبل از خواب   |
| هر زمان       | دلایل شخصی             | تله سرزنش بیهوده در حوادث غیرمترقبه| ثبت فریز و بازگشت بدون اتلاف وقت از فردا |
+---------------+------------------------+------------------------------------+------------------------------------------+
```

---

## ۶. الگوریتم دادرسی و رتبه‌بندی دادگاه بوشیدو (Court Verdict Engine)

در پایان چرخه ۹۰ روزه، تابع `getDeterministicCourtVerdict` بر اساس ۴ پارامتر اصلی، رتبه و حکم نهایی را صادر می‌کند:

$$\text{Grade} = \begin{cases} 
\text{A+} & \text{if } \text{disciplinePercentage} \ge 85\% \\
\text{A} & \text{if } 70\% \le \text{disciplinePercentage} < 85\% \\
\text{B} & \text{if } 50\% \le \text{disciplinePercentage} < 70\% \\
\text{C} & \text{if } \text{disciplinePercentage} < 50\% \text{ and standardDays} > 20 \\
\text{F} & \text{if } \text{disciplinePercentage} < 50\% \text{ and standardDays} \le 20 
\end{cases}$$

---

## ۷. معماری پایگاه داده و همگام‌سازی دوگانه (Dual-Engine Storage Architecture)

سیستم دارای دو لایه ذخیره‌سازی همگام است:
1. **لایه رابط کاربر (Client Storage):** `LocalStorage` + `IndexedDB` با فلگ `isSynced: boolean`.
2. **لایه سرور (Backend Database):**
   - **موتور اولیه:** PostgreSQL از طریق Prisma ORM.
   - **موتور پشتیبان خودکار (Fallback Engine):** فایل ماندگار محلی `bushido_local_db.json`.

```text
[عملیات در فرانت‌اند (تیک زدن یا کالبدشکافی)]
               |
               +---> ذخیره آنی در LocalStorage (Zero Latency)
               |
               +---> آیا اینترنت متصل است؟
                        |
            +-----------+-----------+
            | بله                   | خیر
            v                       v
[ارسال به API سرور]          [علامت‌گذاری isSynced: false]
            |                       |
[ذخیره در Prisma / JSON]      [تلاش مجدد در برقراری اتصال بعدی]
```

---

## ۸. جدول مشخصات APIهای بک‌اند (RESTful Endpoints)

| مسیر (Endpoint) | متد | شرح عملکرد | سطح دسترسی |
| :--- | :--- | :--- | :--- |
| `/api/auth/register` | `POST` | ثبت‌نام کاربر با ایمیل یا موبایل | عمومی |
| `/api/auth/login` | `POST` | ورود کاربر و صدور JWT | عمومی |
| `/api/cycles` | `GET / POST` | دریافت چرخه‌ها یا ایجاد چرخه جدید | کاربر احرازشده |
| `/api/daily-logs` | `GET / POST` | دریافت و به‌روزرسانی لاگ‌های روزانه | کاربر احرازشده |
| `/api/ai/autopsy` | `POST` | صدور تحلیل کالبدشکافی شکست | کاربر احرازشده |
| `/api/ai/verdict` | `POST` | صدور حکم رسمی دادگاه ۹۰ روزه | کاربر احرازشده |
| `/api/payment/request` | `POST` | درخواست شناسه پرداخت زرین‌پال / شاپرک | کاربر احرازشده |
| `/api/payment/verify` | `POST` | راستی‌آزمایی تراکنش بانکی و ارتقای VIP | کاربر احرازشده |
| `/api/admin/stats` | `GET` | دریافت آمار کلان سیستم | فقط مدیر (Admin) |
| `/api/admin/users` | `GET` | لیست و ویرایش کاربران سیستم | فقط مدیر (Admin) |
| `/api/admin/users/:id` | `PUT` | تغییر سطح اشتراک و نقش کاربر | فقط مدیر (Admin) |
| `/api/admin/subscriptions` | `GET` | گزارش حسابرسی تراکنش‌ها | فقط مدیر (Admin) |
