# 🔒 سند و سیاست‌های امنیتی بوشیدو (Security Policy & Environment Separation Contract)

> **سطح سند:** محرمانه و عملیاتی  
> **مرجعیت:** پروتکل‌های امنیتی، مکانیزم‌های تفکیک محیط، قرارداد قابلیت‌های تستی و قواعد پروداکشن

---

## ۱. ماتریس تفکیک محیط‌ها و قرارداد سرور (Environment Capability Matrix)

پروژه بوشیدو از معماری مبتنی بر قرارداد قابلیت‌های سرور‌محور (`Server-Authoritative Capabilities`) استفاده می‌کند. تمامی تصمیم‌گیری‌های امنیتی در بک‌اند اعتبارسنجی می‌شوند:

| قابلیت / دسترسی | توسعه محلی (`development`) | تست خصوصی Vercel (`ALLOW_TEST_SHORTCUTS=true`) | پروداکشن عمومی / لیارا (`ALLOW_TEST_SHORTCUTS=false`) |
| :--- | :---: | :---: | :---: |
| **ورود سریع (Quick Login)** | ✅ فعال | ✅ فعال | ❌ قفل قطعی (HTTP 403) |
| **OTP شبیه‌سازی‌شده (Mock OTP)** | ✅ فعال | ✅ فعال | ❌ قفل و رد درخواست (HTTP 503) |
| **دیباگ کد OTP در خروجی API** | ✅ با `ENABLE_OTP_DEBUG=true` | ✅ با `ENABLE_OTP_DEBUG=true` | ❌ غیرفعال قطعی |
| **شبیه‌ساز پرداخت (Mock Gateway)** | ✅ فعال | ✅ فعال | ❌ نیازمند درگاه زرین‌پال معتبر |
| **سید پیش‌فرض کاربر/ادمین** | ✅ فعال | ✅ فعال | ❌ بدون اکانت پیش‌فرض خودکار |
| **الزام کلید امن JWT (حداقل ۳۲ کاراکتر)** | ⚠️ با کلید منعطف محلی | ⚠️ با کلید منعطف تست | 🚨 اجباری (در صورت غیبت کرش می‌کند) |
| **هدرهای امنیتی (CSP & Frame-Ancestors)** | 🌐 منعطف برای آی‌فریم و پیش‌نمایش | 🌐 منعطف برای آی‌فریم و پیش‌نمایش | 🛡️ هاردن‌شده و بسته (`SAMEORIGIN`) |
| **بررسی سلامت عمومی (`/api/health`)** | 🔍 وضعیت حداقلی لایونس | 🔍 وضعیت حداقلی لایونس | 🔍 وضعیت حداقلی لایونس |
| **دیاگنوستیک کامل سرور (`/api/admin/diagnostics`)** | 🔐 نیازمند توکن معتبر ادمین | 🔐 نیازمند توکن معتبر ادمین | 🔐 نیازمند توکن معتبر ادمین |

---

## ۲. قوانین تجزیه سخت‌گیرانه مقادیر بولی (Strict Boolean Parsing)

متغیرهای محیطی نظیر `ALLOW_TEST_SHORTCUTS`، `ENABLE_OTP_DEBUG` و `ENABLE_QUICK_LOGIN` با تابع اعتبارسنجی سخت‌گیرانه `parseStrictBoolean` ارزیابی می‌شوند:
- صرفاً رشته دقیق `"true"` (بدون حساسیت به حروف بزرگ و با حذف فاصله‌های اضافی) به عنوان مقدار `true` پذیرفته می‌شود.
- مقادیر خالی، `"1"`، `"yes"`، `"false"`، یا هر مقدار متفرقه به عنوان `false` تفسیر شده و سیستم به حالت امن (**Fail-Closed**) می‌رود.

---

## ۳. سیاست میانبرهای تستی و ورود سریع (Test Shortcuts & Quick Login Lock)

1. **قفل قطعی در پروداکشن (Fail-Closed Default):**
   - در حالت `NODE_ENV=production`، متد `allowTestShortcuts()` خروجی `false` تولید می‌کند، مگر اینکه متغیر محیطی `ALLOW_TEST_SHORTCUTS=true` به صورت صریح و آگاهانه روی سرور ست شده باشد.
   - کلیه میانبرهای دور زدن رمز عبور، شبیه‌سازهای لاگین فوری ادمین و سناریوهای تستی در فرانت‌اند و بک‌اند با این فلگ قفل می‌شوند.

2. **عدم امکان جعل ادمین در کلاینت:**
   - کلاینت اجازه ایجاد توکن یا نشست ادمین ساختگی بدون تایید سرور را در محیط پروداکشن ندارد. درخواست‌های ورود سریع مستقیماً به API سرور ارسال شده و در صورت عدم مجاز بودن محیط بلافاصله رد می‌شوند.

3. **هش کلمه عبور با استاندارد بالا (PBKDF2):**
   - ذخیره‌سازی گذرواژه‌ها با Salt رندوم ۱۶ بایتی کریپتوگرافیک و ۱۰۰,۰۰۰ دور اجرای الگوریتم SHA-512 انجام می‌شود.
   - مقایسه هش‌ها با متد `crypto.timingSafeEqual` برای جلوگیری از حملات زمان‌بندی (Timing Attacks) پیاده‌سازی شده است.

4. **کنترل و ریت لیمیت (Rate Limiting):**
   - تمامی مسیرهای عمومی تحت پوشش محافظت در برابر اسپم و حملات Brute-force قرار دارند.
   - ورود و ثبت‌نام دارای سقف سخت‌گیرانه برای جلوگیری از حدس مکرر رمز عبور است.

---

## ۴. چک‌لیست انتشار پروداکشن نهایی (Public Release Checklist)

قبل از عمومی کردن دامین روی لیارا یا سرور اصلی:
- [ ] متغیر `ALLOW_TEST_SHORTCUTS` حذف شود یا روی `false` قرار گیرد.
- [ ] متغیر `ENABLE_OTP_DEBUG` خاموش باشد.
- [ ] متغیر `ENABLE_QUICK_LOGIN` حذف شود.
- [ ] متغیر `JWT_SECRET` با یک کلید تصادفی پروداکشن با طول حداقل ۳۲ کاراکتر مقداردهی شود.
- [ ] متغیر `DATABASE_URL` (اتصال به دیتابیس پایدار Neon/PostgreSQL) تنظیم شده باشد.
- [ ] کلمه عبور پیش‌فرض مدیر (`SUPER_ADMIN_PASS`) در متغیرهای سرور با یک گذرواژه قوی (حداقل ۱۲ کاراکتر) تعیین گردد.
- [ ] در صورت اتصال درگاه بانکی، متغیر `ZARINPAL_MERCHANT_ID` تنظیم شود.

---

## ۵. امنیت معماری شبیه‌سازی کاربر (Impersonation Architecture & Defense in Depth)

قابلیت ورود موقت و پشتیبانی به عنوان کاربر هدف توسط ادمین (Impersonation) تحت چارچوب امنیتی سخت‌گیرانه پیاده‌سازی شده است:

1. **ممنوعیت شبیه‌سازی مدیر توسط مدیر (Block Admin-to-Admin Impersonation):**
   - تلاش هر مدیر برای شبیه‌سازی اکانت مدیر دیگر با خطای قطعی `403 Forbidden` و کد پایدار `ADMIN_TARGET_IMPERSONATION_FORBIDDEN` مسدود می‌شود.
2. **دفاع در عمق لایه میان‌افزار (`adminMiddleware` Defense-in-Depth):**
   - تمامی روت‌های حساس مدیریتی (`/api/admin/*`) توسط میان‌افزار مرکزی بررسی می‌شوند.
   - هر توکنی با شناسه `isImpersonated: true` (بدون توجه به فیلد `isAdmin` در پی‌لود) با کد خطای `IMPERSONATION_ACCESS_FORBIDDEN` رد می‌شود. هیچ کاربر عادی، کاربر عادی شبیه‌سازی‌شده یا هدف ادمین شبیه‌سازی‌شده هرگز به پنل و روت‌های مدیریت دسترسی پیدا نخواهد کرد.
3. **خروج امن بدون شکست (Fail-Safe Exit Contract):**
   - چرخه خروج از شبیه‌سازی ابتدا توکن ذخیره‌شده ادمین را از طریق روت `/api/auth/me` یا روت اختصاصی `/api/admin/impersonate/exit` اعتبارسنجی می‌کند.
   - **دسته‌بندی قطعی پاسخ‌ها:**
     - `AUTH_REVOKED`: تنها در صورت رد قطعی احراز هویت (کد ۴۰۱، `SESSION_REVOKED`، `INVALID_TOKEN`، `USER_NOT_FOUND`). توکن‌ها پاک شده و کاربر به صفحه ورود منتقل می‌شود.
     - `INVALID_ADMIN_IDENTITY`: در صورتی که اکانت معتبر باشد اما سطح دسترسی مدیریت نداشته باشد. توکن‌ها پاک شده و خروج کامل انجام می‌شود.
     - `TEMPORARY_SERVER_ERROR`: برای خطاهای موقت سرور (۴۲۹، ۵۰۰، ۵۰۲، ۵۰۳، ۵۰۴، ۴۰۸). توکن مدیر و متادیتای شبیه‌سازی کاملاً حفظ شده، اکانت تغییر نکرده و خطای قابل‌تلاش مجدد فارسی نمایش داده می‌شود.
     - `NETWORK_ERROR`: در صورت قطع اینترنت یا خطای شبکه فچ، توکن مدیر حفظ شده و ادعای موفقیت خروج ثبت نمی‌شود.
4. **ثبت وقایع امنیتی بدون نشت اطلاعات حساس (PII-Free Audit Logging):**
   - رخدادهای ورود، رد دسترسی و خروج از شبیه‌سازی در ماژول لاگ حسابرسی ساختاریافته ثبت می‌شوند.
   - نام، شماره تماس، ایمیل یا پی‌لودهای خصوصی هرگز در لاگ‌های سیستمی ذخیره نمی‌شوند.
   - فیلد `targetUserId` در رخداد خروج (`impersonation_exited`) به عنوان متادیتای گزارش‌شده از سوی کلاینت (client-reported metadata) ثبت شده و به عنوان وضعیت سرورمحور تلقی نمی‌گردد.

---

## ۶. بدهی امنیتی موکول‌شده به فازهای آتی (Deferred Security & Release Hardening Debt)

- **ذخیره‌سازی توکن حامل در حافظه جاوااسکریپت (JavaScript-Accessible Bearer-Token Storage):**
  - **وضعیت فعلی:** توکن‌های احراز هویت و شبیه‌سازی در `localStorage` و `sessionStorage` نگهداری می‌شوند که در دسترس جاوااسکریپت کلاینت قرار دارد.
  - **ریسک مرتبط:** در صورت بروز آسیب‌پذیری احتمالی تزریق اسکریپت (XSS)، امکان دستیابی به توکن احراز هویت وجود دارد.
  - **برنامه ارتقا در فاز سخت‌سازی:** مهاجرت به کوکی‌های امن سرورمحور با پرچم‌های `HttpOnly`, `Secure`, `SameSite=Strict` به همراه محافظت ضد جعل درخواست (CSRF Token / Origin Verification) برای حذف کامل دسترسی مستقیم اسکریپت به توکن‌های سشن.


