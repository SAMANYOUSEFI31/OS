import React from 'react';

/**
 * Inactive contract stub retained for data-integrity and import-removal contract tests.
 * All runtime routes for `/database` are canonicalized and routed directly to ArchivesView
 * via `routerUtils.ts` and `App.tsx`. This stub is never mounted to active navigation.
 */
export const DatabaseView: React.FC = () => null;
