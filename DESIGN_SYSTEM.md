# 🥋 سند جامع سیستم طراحی و حاکمیت بصری بوشیدو (Bushido OS Master Design System & Governance)
> **نسخه:** ۳.۰ (Master Design System & Single Source of Truth Edition)  
> **مرجع فنی، معماری و حاکمیت طراحی برند:** Bushido Discipline OS  
> **مخاطب:** تمامی توسعه‌دهندگان فرانت‌اند، طراحان محصول و عامل‌های کدنویسی هوش مصنوعی (AI Coding Agents)  
> **هدف:** استقرار تک‌مرجع حقیقت (SSOT) برای هویت بصری، سلسله‌مراتب توکن‌ها، انضباط سمانتیک، بنچ‌مارک‌های کنتراست ادراکی (W3C APCA و WCAG 2.2 AAA)، الگوهای تعاملی (Affordance)، مقیاس‌های هندسی و قوانین قطعی حاکمیت هوش مصنوعی جهت پیشگیری از هرگونه انحراف بصری (Anti-Drift).

---

## ۱. هویت محصول و فلسفه بنیادین (Product Identity & Core Philosophy)

سامانه انضباط شخصی بوشیدو (**Bushido Discipline OS**) یک ابزار تفننی، ردیاب عادت اسباب‌بازی، شکلات انگیزشی یا بازی آرکید رنگارنگ نیست. بوشیدو یک **سیستم‌عامل انضباط فردی و زره دیجیتال** برای پشتیبانی از ۶ رکن بنیادین است:
1. **انضباط و تسلط بر نفس (Discipline)**
2. **پاسخگویی و تعهد بی‌قیدوشرط (Accountability)**
3. **مالکیت کامل بر اعمال و نتایج (Ownership)**
4. **تداوم و پایداری پولادین (Consistency)**
5. **اجرای بی‌نقص تاکتیکی (Execution)**
6. **کمال و استادی در نبرد روزانه (Mastery)**

### ارکان چهارگانه فلسفه طراحی (Core Design Pillars):
1. **وقار و خویشتنداری رزمی (Martial Stoicism):** حذف تمام زواید آرکیدی، جلوه‌های نئونی اغراق‌آمیز، کانفتی‌های کارتونی و پاپ‌آپ‌های هیجانی کاذب. پیروزی و شکست با وقار و سکوت گزارش می‌شوند.
2. **دقت و وضوح تاکتیکی (Tactical Precision):** استقرار بالاترین استانداردهای کنتراست ادراکی و تایپوگرافی مهندسی‌شده تا در تاریکی شب، دیدگان کاربر کمترین خستگی و بیشترین تمرکز را داشته باشند.
3. **مینیمالیسم صومعه‌ای (Monastic Minimalism):** پرهیز از شلوغی بصری، کارت‌های تو در تو، خطوط سیمی سفید تند و پالت‌های رنگین‌کمانی تصادفی. جرم و عمق سطوح تاریک جایگزین خطوط اضافه می‌شوند.
4. **شفافیت عملیاتی وضعیت-اقدام-عواقب (Operational Clarity):** هر وضعیت، اقدام و پیامدی در سیستم دارای یک توکن معنایی منحصر‌به‌فرد و قطعی است که کاربر بدون هیچ ابهامی وضعیت نبرد خود را درک کند.

---

## ۲. سلسله‌مراتب توکن‌ها و قانون ممنوعیت دور زدن (Token Hierarchy & No-Bypass Rule)

سیستم طراحی بوشیدو از یک ساختار چهار لایه‌ای سخت‌گیرانه پیروی می‌کند. دور زدن این سلسله‌مراتب در کدنویسی اکیداً ممنوع است:

```
[۱. توکن‌های پایه (Primitive Tokens)]
       ↓ (تعریف رنگ‌های خام، پالت‌های پایه زینک، مقادیر رادیوس، زمان‌بندی و گام‌های عددی)
[۲. توکن‌های معنایی (Semantic Tokens)]
       ↓ (تخصیص نقش‌ها: برند، کمال، اجرا، زنجیره، بدهی، فریز، تحلیل)
[۳. توکن‌های کامپوننت (Component Tokens)]
       ↓ (قراردادهای دکمه‌ها، کارت‌های فونداسیون، اینپوت‌ها، مودال‌ها)
[۴. صفحات و ویوها (Screens & Views)]
```

### قانون عدم دور زدن (No-Bypass Policy):
- هیچ کامپوننت، ویو یا کادر رابط کاربری مجاز به استفاده مستقیم از کدهای هگز خام یا کلاس‌های رنگی تصادفی در سطح صفحه نیست.
- ارجاع همواره باید از طریق **توکن‌های معنایی (`tokens.css`)** یا **کلاس‌های استاندارد تعریف‌شده در سیستم** صورت گیرد.

---

## ۳. قوانین حاکمیتی هوش مصنوعی (AI Governance: Allowed vs. Forbidden)

برای تضمین پایداری معماری بصری در چرخه‌های بازنویسی و توسعه توسط AI Coding Agents، مرزهای زیر با قطعیت اعمال می‌شوند:

### ✅ اقدامات مجاز و الزامی برای هوش مصنوعی (Allowed & Mandatory):
1. **مصرف توکن‌ها:** مصرف مستقیم توکن‌های سمانتیک موجود در `src/styles/tokens.css`.
2. **استفاده از سطوح استاندارد:** استفاده از کلاس‌های سطح زینک و آبسیدین برای کانتینرها و سطوح ($Z_0 \to Z_3$).
3. **تک‌مرز یکپارچه:** رعایت تک‌مرز یکنواخت ۱ پیکسلی (`border-zinc-800` / `#27272a` / `.border-standard`).
4. **فارسی‌سازی ارقام:** تبدیل ۱۰۰٪ ارقام به فارسی با `toPersianDigits`.
5. **تک‌خطی بودن دکمه‌ها و تگ‌ها:** استفاده از `whitespace-nowrap` در تمامی دکمه‌ها، چیپ‌ها و بج‌ها.
6. **پیچش طبیعی متون بدنه:** پشتیبانی از پیچش متنی طبیعی (`leading-relaxed text-right`) در متون توضیحی بدون `truncate`.
7. **انضباط تایپوگرافی تله‌متری:** استفاده از فونت `font-mono` منحصراً برای اعداد شاخص‌ها، تایمرها و مقادیر تله‌متری.
8. **بازاستفاده از قراردادها و الگوها:** پیروی دقیق از قراردادهای کامپوننت‌ها (دکمه‌ها، اینپوت‌ها، کارت‌ها و مودال‌ها).

### 🚫 اقدامات اکیداً ممنوع برای هوش مصنوعی (Forbidden & Banned):
1. **اختراع رنگ‌های جدید:** ایجاد کدهای هگز جدید یا استفاده از پالت‌های بنفش/آبی نئونی خارج از توکن‌های سمانتیک.
2. **استفاده مستقیم از کلاس‌های پالت تیل‌ویند به عنوان وضعیت:** ممنوعیت مطلق کلاس‌هایی مانند `bg-red-500`، `text-green-400` یا `bg-amber-400` به صورت خام بدون تطبیق با توکن سمانتیک.
3. **رادیوس‌های تصادفی:** استفاده از شعاع‌های دلخواه خارج از مقیاس ۷ مرحله‌ای استاندارد (مانند `rounded-[17px]` یا `rounded-[22px]`).
4. **مقیاس‌های فاصله‌گذاری دلخواه:** استفاده از فاصله‌ها و گپ‌هایی خارج از گام‌های ۴ پیکسلی (۴، ۸، ۱۶، ۲۴، ۳۲).
5. **حاشیه‌های دوگانه، وایرفریمینگ و کادربندی‌های تیز (Dual Borders, Stark Wireframes & Sharp Boxification):**
   - *کارت‌های اصلی استاندارد با کرو نرم و بردر ملایم (`radius-card: 14px`, `.border-standard`) کاملاً مجاز و الزامی هستند* (مانند کادر شاخص انضباط، کادر عادات و کادرهای KPI).
   - *آنچه اکیداً ممنوع است*: کشیدن کادرهای مستطیلی با گوشه‌های ۹۰ درجه تیز بدون کرو، بردرهای سفید تند، خطوط جداکننده سراسری افقی/عمودی داخل کارت‌ها (`border-t`, `border-b`)، کادرهای مستطیلی تیز تودرتو یا محصور کردن المان‌های گرافیکی و متنی در جعبه‌های خط‌کشی‌شده خشک. تفکیک درونی باید با تغییر ملایم سطوح نوری ($Z_0 \to Z_1 \to Z_2$) و گپ‌های حساب‌شده صورت گیرد.
6. **پالت رنگین‌کمانی برای عادات (Rainbow Habit Cards):** رنگ‌آمیزی تصادفی ۵ رکن فونداسیون.
7. **سایه‌ها و هالوژن‌های نئونی جدید:** اعمال `shadow-xl`، `shadow-2xl`، جلوه‌های نئونی گسترده و سایه‌های رنگی در کدهای جدید.
8. **استثناهای بصری تک‌کامپوننتی:** ایجاد استایل‌های بصری استثنایی و یکتا برای یک کامپوننت خاص بدون ارتقای کل سیستم.
9. **نمودارهای ستونی میله‌ای وایرفریمی در پنل مدیریت:** استفاده از میله‌های بریده‌بریده مستطیلی در قفس‌های سیمی با خط‌کشی‌های خشک به جای نمودارهای پیوسته اسپلاین با انحنای نرم بزیه (Spline Curved Area Charts).
10. **تکثیر تب‌های زائد در مدیریت:** ایجاد تب‌های جداگانه نامتجانس یا تکثیر متریک‌های ماندگاری (Retention) در تب جداگانه؛ داشبورد مدیریت منحصراً ساختار ۳ تبه یکپارچه دارد.

---

## ۴. اصل ضد انحراف بصری (The Anti-Drift Principle)

> **قانون طلایی ضد انحراف:**  
> اگر یک تغییر بصری یا تنظیمی بر بیش از یک خانواده کامپوننت اثر می‌گذارد، **باید توکن پایه در `src/styles/tokens.css` تغییر کند**؛ اعمال وصله‌های موضعی، تغییر هگز در یک کامپوننت منفرد یا شخصی‌سازی تک‌صفحه‌ای اکیداً ممنوع است.

---

## ۵. مالکیت انحصاری توکن‌های رنگی معنایی (Semantic Color Ownership)

هر رنگ اشباع در سامانه بوشیدو دارای **یک نقش و معنای انحصاری و غیرقابل انتقال** است؛ هیچ بازاستفاده متفرقه‌ای خارج از این تعریف مجاز نیست:

| توکن معنایی | کد هگز مرجع | کلاس معادل | نقش انحصاری در سیستم | رفتار در حالت تعامل / فعال |
| :--- | :--- | :--- | :--- | :--- |
| **Discipline Crimson**<br>(سرخ اصیل بوشیدو) | `#E11D48`<br>(`rgb: 225, 29, 72`) | `text-rose-500`<br>`bg-[#E11D48]` | **هسته هویت برند، تعهدات اصلی، تب فعال ناوبری، فوکوس سیستم** | `hover:bg-[#BE123C]`<br>`ring-rose-500/40` |
| **Samurai Amber**<br>(طلای خرد سامورایی) | `#FBBF24`<br>(`rgb: 251, 191, 36`) | `text-amber-400`<br>`bg-amber-500/10` | **کمال تعهد (۱۰/۱۰)، قضاوت سنسی AI، امتیاز تجمعی کل، اشتراک VIP** *(هرگز برای موفقیت عادی روزانه به کار نمی‌رود)* | `border-amber-500/30`<br>`hover:bg-amber-500/20` |
| **Vitality Emerald**<br>(سبز فتح و حیات) | `#34D399`<br>(`rgb: 52, 211, 153`) | `text-emerald`<br>`bg-emerald-subtle` | **تکمیل اجرای روز استاندارد (۵/۵ رکن، ۸/۱۰)، پایداری عادات، چک‌باکس‌های تایید شده** | `border-emerald-subtle`<br>`hover:bg-emerald-subtle/80` |
| **Tactical Orange / Flame**<br>(آتش تاکتیکی زنجیره استمرار) | `#F97316` / `#FB923C`<br>(`rgb: 249, 115, 22`) | `text-orange`<br>`bg-orange-subtle` | **زنجیره متوالی فعال (Pure Streak)، شعله استمرار و رکورد زنجیره تاریخی** *(دارای پالت کامل ۱۰ پله‌ای ۵۰ تا ۹۵۰)* | `border-orange-subtle`<br>`text-orange` / `animate-flame-flicker` |
| **Honor Crimson / Alert Red**<br>(سرخ بحران و بدهی) | `#F87171` / `#EF4444`<br>(`rgb: 248, 113, 113`) | `text-debt`<br>`bg-debt-subtle` | **بدهی‌های دیسیپلین، هشدارهای انضباطی، روزهای سوخته/شکست، قفل رفتار** | `border-debt-subtle`<br>`animate-pulse` |
| **Frost Blue**<br>(آبی فریز و توقف) | `#60A5FA`<br>(`rgb: 96, 165, 250`) | `text-blue-400`<br>`bg-blue-500/10` | **توقف اضطراری موجه (مرخصی شخصی، فریز مجاز روز)** | `border-blue-500/30`<br>`hover:bg-blue-500/20` |
| **Bushido Violet**<br>(بنفش تحلیل و خرد) | `#C084FC`<br>(`rgb: 192, 132, 252`) | `text-purple-400`<br>`bg-purple-500/10` | **کالبدشکافی ثبت‌شده (پرونده مختومه)، تحلیل گذشته‌نگر، اصلاحات ساختاری** | `border-purple-500/30`<br>`text-purple-300` |

---

## ۶. پالت سطوح و معماری عمق در محور Z (Surfaces & Z-Axis Luminance Hierarchy)

تفکیک لایه‌ها منحصراً بر پایه گام‌های نوری حساب‌شده (۱ تا ۳ درصد اختلاف روشنایی) در جهت محور Z شکل می‌گیرد:

```
┌────────────────────────────────────────────────────────┐
│  Z0: App Canvas Root (#09090B)                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Z1: Elevated Cards (#121215)                    │  │
│  │  ┌────────────────────────────────────────────┐  │  │
│  │  │  Z2: Sub-Containers & Inputs (#18181B)     │  │  │
│  │  └────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────┘  │
│                                                        │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Z3: Floating Overlays & Modals (#1C1C21)        │  │
│  └──────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────┘
```

- **سطح صفر ($Z_0$ - App Canvas Root):** `#09090B` (`--color-canvas-root` / `.surface-z0`) — بستر اصلی نرم‌افزار، اشباع زیر ۲٪، صفر کردن اثر هاله در شب.
- **سطح پوسته ناوبری (Shell Chrome Surface):** `rgba(9, 9, 11, 0.80)` (`--color-shell-chrome` / `.surface-shell`) — شیشه مات استاندارد اپل (`backdrop-blur-xl` + `saturate-180`) برای هدر فوقانی و نوار تب تحتانی جهت پیوستگی نوری با محتوای در حال اسکرول.
- **سطح یک ($Z_1$ - Elevated Cards):** `#121215` (`--color-card-elevated` / `.surface-z1`) — کانتینرهای اصلی، کارت‌های ۵ رکن فونداسیون، بنتوگریدهای اصلی. *(کد `#131215` صرفاً به عنوان الیاس مجاز ثبت شده است).*
- **سطح دو ($Z_2$ - Sub-Containers & Inputs):** `#18181B` (`--color-card-inner` / `.surface-z2`) — محفظه‌های آیکون، فیلدهای ورودی فرم‌ها، کادرهای یادداشت روزانه و المان‌های درون‌کارت. *(کد `#0C0B0E` صرفاً به عنوان الیاس پنل‌های تو در تو ثبت شده است).*
- **سطح سه ($Z_3$ - Floating Modals, Sheets & Menus):** `#1C1C21` (`--color-card-floating` / `.surface-z3`) — پنجره‌های مودال، منوهای بازشو، شیت‌های شناور.

---

## ۷. فلسفه نورپردازی، سایه‌ها و بردرها (Elevation, Shadows & Borders)

### الف) اولویت درخشندگی بر سایه (Luminance-First Elevation):
- تفکیک لایه‌ها با تغییر ملایم پس‌زمینه انجام می‌شود، نه با سایه‌های نئونی گسترده.
- سایه استاندارد مجاز در کدهای جدید: `0 1px 2px rgba(0,0,0,0.15)` (`--shadow-subtle`).
- **ممنوعیت در کدهای جدید:** استفاده از `shadow-xl`، `shadow-2xl`، جلوه‌های گلو و سایه‌های رنگی در کامپوننت‌های جدید اکیداً ممنوع است.
- **ثبت بدهی فنی (Legacy Glow Debt):** جلوه‌های نورانی گسترده و هالوژن‌های رنگی قدیمی (`.theme-glow`) به عنوان بدهی فنی مستند شده و در کدهای جدید گسترش نمی‌یابند.

### ب) مرزها و خطوط جداکننده (Borders & Dividers):
- **کادر استاندارد:** `#27272A` (`border-zinc-800` / `--color-border-subtle` / `.border-standard`). تک‌پیکسلی، مات و ضد وایرفریمینگ.
- **کادر هاور:** `#3F3F46` (`border-zinc-700` / `--color-border-hover` / `.border-hover`).
- **کادر فوکوس / فعال:** `#52525B` (`border-zinc-600` / `--color-border-active` / `.border-active`) یا رینگ اختصاصی برند بوشیدو (`ring-2 ring-rose-500/40`).
- **تسطیح عمق با خطوط جداکننده (Flatten Depth Rule):** برای تفکیک بخش‌های داخل کارت، از خط جداکننده افقی (`border-t border-zinc-800`) به جای قرار دادن کارت در دل کارت دیگر استفاده می‌شود.

---

## ۸. سیستم تایپوگرافی دوگانه و مقیاس متنی (Typography & Scaling)

سیستم تایپوگرافی بوشیدو از ترکیب دو قلم تخصصی تشکیل شده است:
1. **قلم لایه انسانی و روایی (`Vazirmatn`):** برای کلیه عناوین، متون بدنه، راهنماها، دکمه‌ها و فارسی‌سازی اعداد.
2. **قلم سنجش و تله‌متری (`JetBrains Mono` / `font-mono`):** منحصراً برای اعداد شاخص‌ها، درصدهای پیشرفت، تایمرها و ارقام مقایسه‌ای با فعال بودن `tabular-nums` (`tnum`).

### مقیاس تایپوگرافی (Master Typography Scale):

| سطح تایپوگرافی | اندازه فونت (px) | ارتفاع خط (px) | وزن فونت (Weight) | موارد استفاده |
| :--- | :--- | :--- | :--- | :--- |
| **Hero** | `42px` (`text-[42px]`) | `48px` (`leading-[48px]`) | Black (۹۰۰) | ارقام درشت روز نبرد و هیرو تالار رکوردها |
| **Headline XL** | `24px` (`text-2xl`) | `32px` (`leading-8`) | ExtraBold (۸۰۰) | سرتیتر بخش‌های اصلی و تالار افتخارات |
| **Headline LG** | `20px` (`text-xl`) | `28px` (`leading-7`) | Bold (۷۰۰) | عناوین مودال‌ها و سرتیتر داشبورد |
| **Section Title** | `14px` (`text-sm`) | `20px` (`leading-5`) | Bold (۷۰۰) | عنوان کارت‌های ۵ رکن و گروه‌های تنظیمات |
| **Subtitle** | `12px` (`text-xs`) | `18px` (`leading-[18px]`) | Regular (۴۰۰) | زیرعنوان‌های راهنما و متون راهنمای فرم |
| **Body** | `14px` (`text-sm`) | `24px` (`leading-6`) | Regular (۴۰۰) | توضیحات عادات، نقد سنسی و بازتاب روزانه |
| **Body Secondary** | `12px` (`text-xs`) | `20px` (`leading-5`) | Regular (۴۰۰) | توضیحات فرعی، متن‌های قوانین و جزئیات |
| **Action** | `13px` (`text-[13px]`) | `16px` (`leading-4`) | SemiBold (۶۰۰) | برچسب دکمه‌ها و کلیدهای عملیاتی |
| **Metric Mono** | `16px` (`text-base font-mono`) | `20px` (`leading-5`) | Bold (۷۰۰) | امتیازها، درصدهای ماتریس، زمان کات‌آف |
| **Micro Badge** | `11px` (`text-[11px]`) | `14px` (`leading-[14px]`) | SemiBold (۶۰۰) | بج‌های میکرو، تگ‌های اطلاعاتی وضعیت |

### کنتراست ادراکی متون بر اساس بنچ‌مارک W3C APCA:
- **متن اصلی و ارقام ($L_c = 104$):** `#F4F4F5` (`text-zinc-100` روی سطوح $Z_0$ و $Z_1$).
- **متن بدنه و توضیحات ($L_c = 78$):** `#A1A1AA` (`text-zinc-400` روی سطوح $Z_1$ و $Z_2$).
- **کپشن‌های فرعی و ارقام مخرج ($L_c = 60$):** `#71717A` (`text-zinc-500`).
- **راهنمای کنتراست زیرنویس‌ها و کپشن‌های بدنه (Caption & Subtitle Contrast Guidance):** در سطور تعاملی نظیر پایه‌های ۵‌گانه عادات و ماموریت ویژه در میدان نبرد، متن‌های توضیحی و زیرنویس‌های ریز شبه‌بدنه (نظیر متون ۱۱ پیکسلی `text-[11px]`) برای حفظ خوانایی روی سطوح تیره باید حتماً از توکن `text-role-secondary` (`#A1A1AA` با کنتراست ادراکی $L_c \ge 78$) یا قوی‌تر استفاده کنند. استفاده از کلاس‌های میوتد ضعیف‌تر (نظیر `text-role-muted` با $L_c \approx 60$) برای متون شبه‌بدنه ۱۱ پیکسلی ممنوع است و کلاس‌های میوتد صرفاً برای ارقام مخرج غیرفعال، برچسب‌های فانتوم و نشانگرهای ساختاری مجاز هستند.

---

## ۹. مقیاس انحنای هندسی و قانون انحنای تو در تو (Corner Radius Hierarchy)

مقیاس انحناها در سیستم بوشیدو تثبیت شده و دارای ۷ گام دقیق است:

| مقدار (px) | کلاس Tailwind | توکن معادل | موارد استفاده |
| :--- | :--- | :--- | :--- |
| **4px** | `rounded` | `--radius-micro` (`.radius-micro`) | چک‌باکس‌های مینیاتوری آرشیو، المان‌های ریز |
| **6px** | `rounded-md` | `--radius-badge` (`.radius-badge`) | تگ‌های اینلاین بسیار ریز و میکرو چیپ‌ها |
| **8px** | `rounded-lg` | `--radius-control` (`.radius-control`) | بج‌ها، چیپ‌های وضعیت، سلول‌های ماتریس ۹۰ روزه |
| **12px** | `rounded-xl` | `--radius-component` (`.radius-component`) | دکمه‌های استاندارد، محفظه آیکون عادات، اینپوت‌ها |
| **14px** | `rounded-[14px]` | `--radius-card` (`.radius-card`) | کارت‌های ۵ رکن فونداسیون، کانتینرهای سطح ۱ |
| **20px** | `rounded-[20px]` | `--radius-modal` (`.radius-modal`) | پنجره‌های مودال، کانتینرهای شناور، شیت‌ها |
| **9999px** | `rounded-full` | `--radius-capsule` (`.radius-capsule`) | دکمه‌های کپسولی، دستگیره‌های اسکرول‌بار |

> **قانون ریاضی انحنای تو در تو (Nested Radius Formula):**  
> $$\text{Inside Corner Radius} = \text{Outside Corner Radius} - \text{Padding}$$  
> به عنوان مثال: اگر کارت اصلی دارای انحنای `20px` (`rounded-[20px]`) و پدینگ `8px` باشد، محفظه داخلی باید دقیقاً دارای انحنای `12px` (`rounded-xl`) باشد تا از تداخل دندانه‌دار هندسی جلوگیری شود.

---

## ۱۰. مقیاس فاصله‌گذاری و ابعاد کانتینرها (Spacing & Layout Dimensions)

- **پایه شبکه:** سیستم بر پایه گام‌های ۴ پیکسلی طراحی شده است (`--spacing-base: 4px`).
- **گام‌های مجاز برای شکاف‌ها (Gaps):** منحصراً مقادیر **۴، ۸، ۱۶، ۲۴ و ۳۲ پیکسل** (`gap-1`, `gap-2`, `gap-4`, `gap-6`, `gap-8` / `--gap-4`, `--gap-8`, `--gap-16`, `--gap-24`, `--gap-32`).
- **سقف عرضی کانتینرها (Max Layout Bounds):**
  - کانتینر استاندارد داشبورد: `max-w-5xl` (1024px / `--layout-content-max`).
  - کانتینر گسترده اتاق فرماندهی: `max-w-6xl` (1152px / `--layout-wide-max`).

---

## ۱۱. مدل وضعیت کامپوننت‌ها (Component State Model)

کلیه کامپوننت‌های تعاملی از یک چرخه وضعیت شش‌گانه با ترتیب اولویت ریاضی پیروی می‌کنند:

```
[Default] → [Hover] → [Active] → [Focus Visible] → [Disabled] → [Loading]
```

### قانون اولویت وضعیت‌ها (State Priority Order):
$$\text{Disabled} > \text{Loading} > \text{Active} > \text{Focus Visible} > \text{Hover} > \text{Default}$$

1. **وضعیت پیش‌فرض (Default):** حالت پایدار با استایل سمانتیک مشخص.
2. **وضعیت هاور (Hover):** تغییر ملایم مرز به `--color-border-hover` (`#3F3F46`) در ۱۵۰ میلی‌ثانیه.
3. **وضعیت فوکوس کیبورد (Focus Visible):** نمایش حلقه متمایز حداقل ۲ پیکسلی (`ring-2 ring-rose-500/50 ring-offset-2 ring-offset-[#09090b]`).
4. **وضعیت فعال/فشرده (Active):** مقیاس فیزیکی `active:scale-[0.98]`.
5. **وضعیت بارگذاری (Loading):** غیرفعال‌سازی اشاره‌گر (`pointer-events-none`) با نمایش اسپینر ظریف یا اسکلتون بدون تغییر هندسه دکمه.
6. **وضعیت غیرفعال (Disabled):** شفافیت ۴۰٪ (`opacity-40`)، عدم پذیرش اشاره‌گر (`cursor-not-allowed pointer-events-none`) و غیرفعال شدن کامل اکشن‌ها.
- **قانون:** افزودن هر وضعیت بصری مازاد بدون مستندسازی در سیستم طراحی اکیداً ممنوع است.

---

## ۱۲. سیستم موشن و فیزیک انیمیشن (Motion System)

انیمیشن در بوشیدو ابزار تزئینی نیست؛ موشن صرفاً برای **تایید عمل، انتقال سلسله‌مراتب و تغییر وضعیت** به کار می‌رود:

### توکن‌های زمانی موشن (Motion Tokens):
- **Instant (۱۰۰ms - `--duration-instant`):** بازخورد لمسی میکرو، کلیک‌های دکمه، تغییر وضعیت چک‌باکس‌ها.
- **Fast (۱۵۰ms - `--duration-fast`):** تعاملات دکمه‌ها، کارت‌های عادات و فیلدهای ورودی فرم‌ها.
- **Regular (۲۰۰ms - `--duration-regular`):** فیلترینگ ماتریس ۹۰ روزه، جابه‌جایی تب‌ها و تغییر صفحات.
- **Deliberate (۳۰۰ms - `--duration-deliberate`):** باز و بسته شدن پنجره‌های مودال و شیت‌های شناور.

### شتاب و فیزیک حرکتی:
- شتاب فنری کنترل‌شده: `cubic-bezier(0.22, 1, 0.36, 1)` (`--ease-spring`).
- شتاب تاکتیکی مستقیم: `cubic-bezier(0.4, 0, 0.2, 1)` (`--ease-tactical`).
- **قانون کاهش حرکت (Reduced Motion):** سیستم به طور کامل از `prefers-reduced-motion: reduce` تبعیت کرده و در صورت فعال بودن، کلیه انیمیشن‌ها آنی (`0.01ms`) خواهند شد.

---

## ۱۳. استانداردهای دسترسی‌پذیری و کنتراست (Accessibility - a11y & Contrast)

1. **خوانایی و کنتراست APCA:** هیچ افت کنتراست تزئینی مجاز نیست؛ متون اصلی باید $L_c \ge 104$ و متون بدنه $L_c \ge 78$ باشند.
2. **فوکوس اجباری و ملموس:** فوکوس کیبورد برای تمامی المان‌های تعاملی با کلاس متمرکز `focus-ring-tactical` یا `focus-ring-neutral` اجباری است، حداقل ۲ پیکسل ضخامت دارد و با آفست استاندارد بدون رینگ‌های خشن سفید رندر می‌شود.
3. **اندازه هدف لمسی (Touch Targets Contract - U1):**
   - **الزام عمومی ۴۴ پیکسل:** کلیه المان‌های قابل کلیک، تپ و تعامل روی موبایل (دکمه‌ها، سرچ، چیپ‌های فیلتر، کنترلهای بستن، پاپ‌آپ‌ها و دکمه‌های آیکونی) حداقل دارای مساحت لمسی $44 \times 44$ پیکسل (`min-w-[44px] min-h-[44px]` یا `w-11 h-11`) با کلاس `touch-manipulation` هستند.
   - **کنترل‌های آیکونی ثانویه (Secondary Dismiss / Clear Controls):** برای آیکون‌های ثانویه مانند دکمه بستن راهنمای نخستین ورود (`FirstRunTour`)، بستن بنر سناریوی دمو، بستن راهنمای سوایپ، بستن بنر PWA/iOS و پاک کردن متن جستجو در بایگانی، از تکنیک مارجین منفی (`-my-2.5 -ml-2.5`) همراه با کانتینر ۴۴ پیکسلی استفاده می‌شود تا ابعاد بصری آیکون ظریف بماند اما هدف لمسی بدون خطا در دسترس انگشت کاربر باشد.
   - **قرارداد سطرهای عادات (Habit Row Contract):** تنها کنترل تعاملی سطر عادت، دکمه تمام‌عرض `<button>` با `w-full min-h-[44px]` است. المان استمپ `.entity-stamp-target` صرفاً یک نشانگر بازخورد بصری است و کنترل مجزا نیست.
4. **رفتار مستقل از صدا:** وضعیت‌های برنامه نباید صرفاً به پخش صوت متکی باشند؛ تمام فیدبک‌ها باید معادل بصری داشته باشند.
5. **پشتیبانی از کاهش حرکت (`prefers-reduced-motion: reduce`):**
   - سیستم به طور کامل از تنظیمات کاهش انیمیشن سیستم‌عامل کاربر تبعیت می‌کند.
   - در حالت فعال بودن `reduced-motion`، کلیه ترنزیشن‌ها و انیمیشن‌ها آنی (`0.01ms`) شده و انیمیشن‌های چرخشی، جهشی، لودینگ‌ها و پالس‌ها با `motion-reduce:animate-none` به صورت ایستا نمایش داده می‌شوند.
6. **پشتیبانی از حالت کنتراست بالا (`forced-colors: active`):**
   - در حالت High Contrast سیستم‌عامل (Windows High Contrast Mode)، خطوط مرزی، حلقه‌های فوکوس و متون دکمه‌ها با استفاده از رنگ‌های سیستمی مرورگر (`ButtonText`, `Highlight`, `CanvasText`, `LinkText`) حفظ شده و خوانایی ۱۰۰٪ تضمین می‌گردد.

---

## ۱۴. چگالی اطلاعات و تراکم بصری (Information Density)

بوشیدو یک سیستم‌عامل انضباطی با **چگالی اطلاعاتی بالا (High Density)** است:
- فضای خالی (Whitespace) صرفاً برای ایجاد وضوح، گروه‌بندی منطقی و تفکیک سلسله‌مراتب استفاده می‌شود، نه برای پر کردن صفحه با فضاهای خالی بیهوده.
- داده‌ها، آمارها و ارقام در کنار یکدیگر با فاصله‌گذاری منظم و منسجم چیده می‌شوند.

---

## ۱۴.۱. اصل یکپارچگی سطوح و منع کادربندی‌های قفسه‌ای (Seamless Surfaces & Anti-Boxification)

یکی از اشتباهات بصری در دارک‌مود، قطعه‌قطعه کردن کارت‌ها با خطوط برش افقی (`border-t`, `border-b`) یا محبوس کردن نمودارها و آمارها در جعبه‌های مستطیلی تیز با بردرهای سخت است:
1. **منع خطوط برش داخلی در کارت‌ها:** در داخل یک کانتینر اصلی (`surface-z1 radius-card`)، از کشیدن خطوط مرزی افقی سرتاسری (`border-b border-standard` یا `border-t border-standard`) که کارت را به نوارهای بریده بریده تبدیل می‌کنند اکیداً خودداری شود. تفکیک بخش‌ها باید از طریق فاصله‌گذاری هندسی (`gap` و `padding`) و تایپوگرافی ایجاد شود.
2. **منع قفسه‌بندی ستون‌های نمودار (No Caged Bars):** ستون‌ها، خطوط و داده‌های آماری هرگز نباید در محفظه‌های دارای کادر مستطیلی تیز محبوس شوند.
3. **طراحی ارگانیک و منحنی‌دار نمودارها (Curved Spline Flow):** نمودارهای روند باید دارای خطوط پیوسته و منحنی رِند و نرم (Smooth Spline / Bezier Curves) با گرادیان‌های نوری محو شونده باشند تا جریان پیوسته داده به زیبایی و سادگی درک شود.
4. **سلسله‌مراتب نرم سطوح بدون بردر کاذب:** برای زیربخش‌های درون کارت از سطح `surface-z2` با انحنای متناسب هندسی (`radius-component`) استفاده شود بدون آنکه بردرهای اضافه یا هاور کاذب روی المان‌های غیرکلیکی اضافه شود.

---

## ۱۵. حاکمیت آیکون‌ها و سطوح بصری (Icon Governance & Hierarchy)

- **خانواده انحصاری:** تمامی آیکون‌ها منحصراً از کتابخانه `lucide-react` با استروک ثابت `2px` و به صورت خطی (Outline) وارد می‌شوند.
- **ممنوعیت اختلاط:** ترکیب آیکون‌های توپر (Filled) و خطی (Outline) در یک بخش ممنوع است.
- **نقش آیکون‌ها:** آیکون‌ها نمایانگر کارکرد هستند نه شخصیت تزئینی.

### سلسله‌مراتب ابعادی آیکون‌ها (۵ سطح استاندارد):
- **سطح ۱ (Master Hero):** کانتینر `w-12 h-12` با آیکون `w-6 h-6`.
- **سطح ۲ (Section Header):** کانتینر `w-10 h-10` با آیکون `w-5 h-5` (با رنگ خنثی زینک `text-zinc-200`).
- **سطح ۳ (Interactive Habit Cards):** کانتینر `w-10 h-10 shrink-0` با آیکون `w-5 h-5`.
- **سطح ۴ (Stats & Record Cards):** کانتینر `w-8 h-8 shrink-0` با آیکون `w-4 h-4`.
- **سطح ۵ (Inline Micro):** آیکون `w-3.5 h-3.5` تا `w-4 h-4`.

---

## ۱۶. قواعد محتوا و میکروکپی‌ها (Content & Copywriting Rules)

1. **طول برچسب دکمه‌ها:** برچسب دکمه‌ها ترجیحاً ۱ تا ۳ کلمه و حداکثر ۴ کلمه بوده و هرگز نمی‌شکنند (`whitespace-nowrap`).
2. **بج‌ها و چیپ‌ها:** منحصراً تک‌خطی بدون شکست یا بریدگی.
3. **عدم بریدگی متون تحلیلی (No Truncation for Analysis):** متون توضیحی، پرسش‌های کالبدشکافی، تحلیل‌های سنسی AI و یادداشت‌ها نباید با `truncate` یا سه نقطه ناقص شوند؛ باید با `leading-relaxed text-right` به طور طبیعی بپیچند.
4. **حالت‌های خالی عملیاتی (Operational Empty States):** وضعیت‌های خالی نباید وابسته به تصویرسازی‌های کارتونی باشند؛ ساختار آنها عبارت است از:
   $$\text{Current State} + \text{Clear Explanation} + \text{Next Tactical Action}$$

---

## ۱۷. خلاصه قراردادهای کامپوننت‌ها (Component Contracts)

1. **دکمه‌ها و کلیدهای تعاملی (Buttons):**
   - انواع مجاز:
     - **Primary Crimson:** پس‌زمینه `#E11D48` با متن سفید برای اکشن اصلی.
     - **Mastery Amber:** پس‌زمینه زرد کهربایی (`#FBBF24`) برای عملیات ارتقای VIP و تسلط.
     - **Surface Z2:** پس‌زمینه سطح ۲ (`#18181B`) با بردر `#27272A` برای اقدامات ثانویه.
     - **Ghost:** پس‌زمینه شفاف با هاور سطح ۲.
   - انحنا: `12px` (`rounded-xl` / `--radius-component`).
   - تعامل: `active:scale-[0.98]` و `cursor-pointer`.
2. **کارت‌های عادات روزانه (Habit Cards):**
   - ساختار: `Icon Container` $\to$ `Title & Description` $\to$ `Action Checkbox`.
   - پس‌زمینه: سطح $Z_1$ (`#121215`) با بردر `#27272A` برای تمامی ۵ رکن بدون تفاوت رنگی اولیه (Anti-Rainbow).
   - وضعیت تکمیل‌شده: آیکون و چک‌باکس منحصراً با توکن‌های زمردی (`#34D399` / `emerald-400`).
   - **وضعیت‌های کنترل تکمیل عادات (Habit Completion Control States):**
     1. **حالت ثبت‌نشده (Unchecked):** پس‌زمینه خنثی `surface-z1`، کادر `border-standard`، آیکون مات `text-role-muted` روی `surface-z2`، تمبر مدور توخالی.
     2. **حالت ثبت‌شده (Checked - فونداسیون):** سیگنال منسجم زمردی با فیل `bg-emerald-subtle`، کادر `border-emerald-subtle`، آیکون `text-emerald` و تمبر مدور آرام `border-emerald text-emerald bg-emerald-subtle` بدون خط سفید مازاد.
     3. **ماموریت شتاب‌دهنده ویژه (Special Mission):** وفاداری انحصاری به سمانتیک کمال کهربایی با فیل `bg-amber-subtle`، کادر `border-amber-subtle`، آیکون `text-amber` و تمبر کهربایی.
     4. **تک‌سیگنال هماهنگ (Anti-Double Screaming):** تایید بصری در کل سطح کارت تنیده می‌شود؛ تمبر پایانی به جای دوشاخگی رنگی جیغ، آرام و هماهنگ با بدنه عمل می‌کند.
     5. **ارگونومی و فیزیک لمسی:** هدف لمسی بالای ۴۴ پیکسل، عدم پرش ساختاری با بردر ۱ پیکسلی پایدار و پایداری در تپ‌های متوالی سریع (`latestActiveLogRef`).
3. **فیلدهای ورودی و فرم‌ها (Inputs):**
   - پس‌زمینه: سطح $Z_2$ (`#18181B`) با بردر استاندارد `#27272A`.
   - فوکوس: مرز فعال `#52525B` با رینگ برند؛ بدون گلوهای نئونی ضخیم.
4. **بج‌ها و تگ‌های اطلاعاتی (Chips & Badges):**
   - انحنا: `6px` تا `8px`.
   - نشانگر: `cursor-default select-none pointer-events-none`.
5. **پنجره‌های شناور و مودال‌ها (Modals - $Z_3$):**
   - بدنه: سطح $Z_3$ (`#1C1C21`) با انحنای `20px` (`rounded-[20px]`).
   - قفل اسکرول پس‌زمینه با شمارش مرجع (`useBodyScrollLock`).
6. **ماتریس تاکتیکی ۹۰ روزه (Tactical Heatmap 90):**
   - روز جاری در حال نبرد: زینک خنثی با رینگ طلایی (`ring-2 ring-amber-400`).
   - روز استاندارد (۵/۵): سبز زمردی (`#34D399`).
   - کمال تعهد (۱۰/۱۰): طلای سامورایی (`#FBBF24`).
   - بدهی باز / روز سوخته: سرخ بحران (`#F87171`).
   - فریز شخصی: آبی ملایم (`#60A5FA`).
   - کالبدشکافی ثبت‌شده: بنفش خرد (`#C084FC`).
7. **سلسله‌مراتب سطح و حوضچه امتیاز روزانه (Daily Score Surface Hierarchy & Gauge Well Contract):**
   - **تفکیک عمق از بوم (Z2 Inner Well):** کانتینر امتیاز روزانه منحصراً روی سطح فرورفته/برجسته $Z_2$ (`surface-z2` + `border-standard` + `radius-component`) مستقر است تا از افتادن یا فرونشستن در کف بوم ($Z_0$) جلوگیری کند.
   - **نوار وضعیت بدون پرش (Zero Jitter Ribbon):** تراشه میانی وضعیت تعهد دارای ارتفاع قطعی (`h-6`) است تا جابجایی بین حالت‌های مختلف («در انتظار ۵ پایه»، «روز استاندارد»، «کمال تعهد») هیچ‌گونه پرش عمودی در کارت ایجاد نکند.
   - **قطعات گیج ۱۰ مرحله‌ای:** قطعات پرشده دارای پس‌زمینه سمانتیک و کادر شفاف (`border-transparent`)؛ قطعات خالی دارای سطح خنثی آرام (`surface-z1` روی ول `surface-z2`). پایداری مدل جعبه بدون حاشیه‌های مضاعف.
8. **کنترل‌ها و حالات بصری ثبت عادات (Habit Completion Visual States Contract):**
   - **پایداری سطح کارت (Neutral Card Invariance):** سطح بدنه کارت در هر دو حالت ثبت‌شده و نشده همواره روی زمینه خنثی (`surface-z1` + `border-standard`) باقی می‌ماند. از پر کردن کل کارت با پس‌زمینه زمردی یا کهربایی اکیداً اجتناب می‌شود.
   - **تک‌افوردنس شفاف استامپ و آیکون (Stamp & Icon Signal):** ثبت عادت صرفاً از طریق تغییر رنگ استامپ حلقوی (`bg-emerald-subtle border-emerald-subtle text-emerald`) و ظرف آیکون (`bg-emerald-subtle text-emerald`) نمایان می‌شود. ماموریت ویژه از توکن‌های کهربایی (`amber`) به روش یکسان بهره می‌برد.
   - **ارگونومی لمسی:** مساحت لمسی حداقل ۴۴ پیکسل (`min-h-[44px]`)، فیدبک فشرده‌سازی کنترل‌شده (`active:scale-[0.99]`) و رینگ فوکوس صفحه‌کلید بدون رینگ‌های مزاحم سفید در حالت غیرفوکوس.
9. **چیدمان پشته‌ای ۵ رکن فونداسیون (Foundation Habit List Layout Contract):**
   - **پشته تک‌ستونه استاندارد (Single-Column Stack):** ۵ رکن فونداسیون در یک ستون عمودی یکپارچه (`flex flex-col gap-2.5 sm:gap-3`) در محدوده حداکثر عرض محتوای نبرد مستقر می‌شوند؛ با این ساختار، حفره خالی ناشی از رها شدن آیتم پنجم در شبکه‌های ۲ یا ۳ ستونه تبلت/دسکتاپ به کلی حذف می‌شود.
   - **استقلال ماموریت شتاب‌دهنده:** ماموریت ویژه روز به‌صورت یک ردیف مستقل و تمام‌عرض با تفکیک بصری در زیر ۵ رکن فونداسیون قرار می‌گیرد.
10. **پوسته ناوبری بالا و کپسول شناور همراه (Unified Shell Chrome & Floating Capsule Contract):**
   - **سطح و مات‌شدگی واحد شیشه‌ای (Stoic Smoky Obsidian Glass):** هدر چسبان بالا و کپسول شناور همراه از توکن زمینه واحد (`surface-shell` معادل `rgba(9, 9, 11, 0.92)`) با چگالی تیرگی ۹۲٪ و مات‌شدگی کریستالی (`backdrop-filter: blur(20px) saturate(190%)`) استفاده می‌کنند تا علاوه بر حس مدرن شناور بودن، نویز بصری محتوای زیرین کاملاً محو شده و تمرکز استویک حفظ شود.
   - **کپسول شناور ناوبری همراه (Floating Glass Capsule Navbar):** در نمای همراه، ناوبر به یک کپسول معلق در مرکز پایین صفحه (`radius-capsule` با عرض ارگونومیک `max-w-sm sm:max-w-md`) تبدیل شده است؛ این ساختار دسترسی انگشت شست را در مرکز متمرکز کرده و از کشیدگی بی‌رویه در صفحات بزرگ‌تر (تبلت‌ها) جلوگیری می‌کند.
   - **کادر مرزی مویرگی آرام (Subtle Hairline & No Top Border):** هدر فوقانی در لبه بالای خود هیچ بردر یا خطی ندارد (`border-t-0`) و زمینه شیشه‌ای آن با `pt-safe` تا زیر استاتوس‌بار (ساعت، ناچ، جزیره پویا) کشیده می‌شود؛ خط پایین هدر یک مرز بسیار ظریف مویرگی (`border-subtle`) دارد.
   - **نشانگر تب یکپارچه با امضای سرخ برند (Unified Crimson Tab Indicator):** پیل متحرک تب فعال در ناوبری (`surface-z2 radius-capsule`) به صورت سرتاسری هم آیکون SVG و هم عنوان متنی را پوشش می‌دهد؛ رنگ آیکون و متن تب فعال با سرخ اصیل برند (`text-crimson font-bold / font-black`) مشتعل می‌شود در حالی که تب‌های غیرفعال در رنگ خنثی ثانویه (`text-role-secondary`) آرام می‌گیرند.
   - **سازگاری سراسری با تمام سخت‌افزارهای موبایل و تبلت (Cross-Platform Universal Insets):** انطباق پایدار با `pt-safe` در هدر فوقانی و `pb-safe` در کپسول معلق تحتانی مانع از هرگونه تداخل با جزیره پویا، ناچ، بریدگی دوربین، یا ژست ناوبری اندروید/آیفون می‌گردد.
11. **تفکیک متریک‌های کلان و ثانویه در داشبورد (Dashboard Metrics Anti-Rainbow Contract):**
   - **ارکان تثلیث دیسیپلین (Primary Trinity):** ۳ شاخص اصلی (زنجیره فعال رز، روزهای استاندارد زمردی، مجموع امتیاز کهربایی) با رنگ سمانتیک روی عدد و ظرف آیکون حفظ می‌شوند.
   - **متریک‌های هویتی دسته‌بندی در برابر سنجه بحران (Category Metrics vs Alert Metric):** شاخص‌های دسته‌بندی (کالبدشکافی‌شده با بنفش خرد `text-purple` و توقف اضطراری با آبی فریز `text-blue`) مانند سه رکن اصلی همواره رنگ سمانتیک هویتی عدد و آیکون خود را حفظ می‌کنند تا یکپارچگی بصری بنتوگرید در مقدار صفر مخدوش نشود. در مقابل، شاخص «بدهی کالبدشکافی» یک سنجه هشداری (Incident Metric) است؛ در حالت صفر به نشانه انضباط و آرامش به رنگ خنثی (`text-role-primary` با آیکون خاموش `text-role-muted`) نمایش می‌یابد و صرفاً در صورت وجود بدهی معوق (>۰) به رنگ سرخ بحران (`text-debt`) شعله‌ور می‌گردد تا از ایجاد هشدار کاذب جلوگیری شود.
   - **آرام‌سازی راهنمای نقشه حرارتی (Quiet Heatmap Legend):** پیل‌های راهنما از کادر و متن خنثی استاندارد (`border-standard`، `text-role-secondary`) استفاده می‌کنند و تنها نقطه راهنمای کوچک رنگ وضعیت را نمایش می‌دهد.
12. **فراداده آرام کات‌آف و حذف کادرهای تحمیلی (Quiet Cutoff Meta & Frame Elimination Contract):**
   - **حذف نوار وایرفریم کات‌آف:** حذف کامل ردیف کادر مستطیلی سیمی یا خطوط جداکننده معلق برای کات‌آف شبانه.
   - **ادغام درون هدر ناوبری:** کات‌آف شبانه به‌صورت یک تراشه فراداده آرام (`surface-z2` + `border-standard` + `radius-control` + `text-role-muted`) در کنار برچسب روز در هدر تاریخ نبرد ادغام می‌گردد.
   - **سطح نگارش یادداشت‌ها:** فیلد ورود متن روزانه منحصراً روی سطح تعاملی $Z_2$ (`surface-z2`) برای ایجاد کنتراست خوانا و ارگونومی تایپ مستقر است.
13. **وضعیت‌های صفر-نمونه و داده ناکافی در تحلیل‌ها (Empty Analytics State Contract):**
   - **پرهیز از اعلام وضعیت بحرانی کاذب (No False Crisis Badges):** در صورتی که هنوز داده‌ای برای ارزیابی ثبت نشده باشد (صفر روز فعال یا صفر لاگ در چرخه)، به هیچ عنوان نشان‌ها و بج‌های سرخ بحران یا «آسیب‌پذیر» نباید نمایش داده شوند.
   - **نسخه متنی عملیاتی و خنثی (Neutral Operational Copy):** نمایش برچسب داده ناکافی / در انتظار ثبت (`text-role-secondary` روی `surface-z1` / `surface-z2`) همراه با راهنمای اقدام بعدی برای کاربر (شروع ثبت در میدان نبرد). ساختار و ابعاد چیدمان ثابت می‌ماند و صرفاً منطق ارائه وضعیت تغییر می‌کند.
14. **قانون رنگ‌آمیزی آیکون‌ها: تزیینی در برابر سمانتیک (Icon Coloring Rule: Decorative vs Semantic Contract):**
   - **آیکون‌های تزیینی و سرتیتر بخش‌ها (Decorative Section Icons):** تمام آیکون‌های مستقر در سرتیتر بخش‌ها، عناوین ماژول‌ها و بنرهای ناوبری (نظیر `Compass`، `Trophy`، `Calendar`، `Layers`، `ShieldAlert`) منحصراً از رنگ خنثی ثانویه (`text-role-secondary`) استفاده می‌کنند.
   - **آیکون‌های سمانتیک حامل وضعیت (Semantic Status Icons):** رنگ‌های اشباع (رز زنجیره، زمردی پایداری، کهربایی کمال/ماموریت، سرخ بدهی، آبی فریز، بنفش تحلیل) صرفاً و منحصراً به آیکون‌هایی اختصاص دارند که نشانگر یک وضعیت فعال، نتیجه ارزیابی معنادار یا یکی از اضلاع سه‌گانه مقدس انضباط باشند.
15. **انتخاب‌های بخش‌بندی‌شده و ردیف‌های اقدام در تنظیمات (Settings Segmented Choices & Action Rows Contract):**
   - **انتخاب ساعت کات‌آف (Cutoff Segmented Choices):** گزینه‌ها روی سطح ول $Z_2$ قرار دارند؛ حالت انتخاب‌نشده با سطح $Z_1$، کادر استاندارد و ارتفاع حداقل ۴۴px از وایرفریم‌های خالی تفکیک می‌شود و حالت فعال منحصراً با توکن برند (`bg-crimson-subtle border-crimson-subtle text-crimson`) و تیک تایید مشخص می‌گردد.
   - **ردیف عملیاتی پشتیبان‌گیری (Backup Action Row):** آیتم خروجی داده به عنوان یک کنترل ارگونومیک روی سطح $Z_1$ با هاور $Z_3$، آیکون اختصاصی، متن دوخطی و حداقل ارتفاع ۵۲px عمل می‌کند تا از نوارهای توخالی پرهیز شود.
   - **مهار محدوده خطر (Contained Danger Block):** کادر بازنشانی با سطح $Z_2$ و مرز ملایم بحران (`border-debt-subtle/40`) تفکیک شده و رنگ سرخ صرفاً بر آیکون، عنوان و کلید اقدام نهایی متمرکز است بدون سرخ‌کردن کل بدنه.
16. **سازگاری سطوح پنجره‌های تعاملی و شیارهای پیشرفت (Modals, Auxiliary Cards & Track Surfaces Contract):**
   - **سلسله‌مراتب پله‌ای سطوح مودال‌ها (Modal Surface Escalation):** پنجره‌های شناور (`AuthModal` و `PaymentModal`) روی سطح $Z_3$ و کانتینرهای ول محتوایی آن‌ها روی سطح $Z_2$ قرار دارند؛ کارت‌ها و برچسب‌های درونی مستقر در ول‌ها منحصراً از سطح $Z_3$ با کادر استاندارد بهره می‌برند تا از افت روشنایی به $Z_1$ و ایجاد حس گودی/حفره توخالی در زمینه تیره جلوگیری شود.
   - **انتخاب پلن‌های اشتراک (Subscription Plan Cards):** کارت‌های گزینش‌نشده با سطح $Z_2$، هاور $Z_3$، کادر استاندارد و دکمه‌های رادیویی با پس‌زمینه پرشده $Z_3$ متمایز می‌شوند و از وایرفریم‌ها یا حلقه‌های خالی ۲ پیکسلی پاکسازی شده‌اند.
   - **پایداری شیار نوارهای پیشرفت (Progress Track Invariance):** مسیر زیرین نوارهای پیشرفت در تمام کارت‌های داشبورد و ماتریس وفاداری به جای سطح خام بوم ($Z_0$) منحصراً از سطح محفظه‌ای $Z_2$ یا $Z_3$ با کادر استاندارد استفاده می‌کند تا ترنچ‌های تاریک سوراخ‌مانند در دارک‌مود محو شوند.
17. **ماتریس ارگونومی لمسی موبایل، تفکیک دکمه‌ها از بج‌ها و حاکمیت کپی‌رایتینگ استویک (Mobile-First Touch Ergonomics, Affordance & Copywriting Governance Contract):**
   - **تضمین هدف لمسی حداقل ۴۴ پیکسل برای ۹۹٪ کاربران موبایل:** تمام المان‌های قابل تعامل و تپ (دکمه‌های عادات، ماموریت ویژه، سوئیچ تاریخ نبرد، دکمه‌های کالبدشکافی و منوها) دارای ارتفاع قطعی `min-h-[44px]` یا `min-h-[52px]`، نشانگر دست `cursor-pointer`، فیدبک فشرده‌سازی ملموس (`active:scale-[0.98]` یا `active:scale-[0.99]`) و حریم تعاملی امن هستند.
   - **قرارداد رسمی هدف لمسی سطرهای عادات و ماموریت ویژه (Habit Row Touch-Target Contract):**
     1. **کنترل تعاملی یگانه (Sole Interactive Control):** در پایه‌های پنج‌گانه عادات (Foundation Habits) و ماموریت ویژه (Special Mission)، تنها کنترل تعاملی سطر، دکمه تمام‌عرض `<button>` با ارتفاع حداقل ۴۴ پیکسل (`w-full min-h-[44px]`) است. این امر هدف الزامات Apple HIG (~44pt) و WCAG 2.5.5 (Target Size - Enhanced) را برای کنترل محقق می‌سازد؛ همچنین الزام اندازه هدف WCAG 2.5.8 (Target Size - Minimum) به واسطه اندازه و پهنای کامل سطر پوشش داده می‌شود.
     2. **نشانگر صرفاً بصری `.entity-stamp-target` (Visual Indicator Only):** کلاس و المان `.entity-stamp-target` صرفاً یک نشانگر و بازخورد بصری است و یک کنترل جداگانه محسوب نمی‌شود؛ بنابراین نیازی به داشتن ابعاد ۴۴×۴۴ ندارد (ابعاد دایره‌ای ۲۴ تا ۲۸ پیکسلی آن در داخل سطر به عنوان شاخص وضعیت است).
     3. **ممنوعیت هدف کلیکی مستقل دوم در سطر (No Second Independent Hit Target):** نباید بدون رعایت دقیق ضوابط فاصله و ابعاد استاندارد، کنترل یا هدف کلیکی مستقل دومی در داخل سطر دکمه تعبیه شود.
     4. **کنتراست زیرنویس‌ها و متون توضیحی سطرها (Row Subtitle Contrast):** کلیه متون زیرنویس ۱۱ پیکسلی در سطور عادات و ماموریت ویژه باید برای خوانایی مطلوب روی سطوح تیره از توکن `text-role-secondary` یا قوی‌تر استفاده کنند و از کاربرد کلاس‌های کم‌رنگ‌تر میوتد (`text-role-muted`) برای متون خواندنی بدنه پرهیز شود.
   - **تفکیک قطعی دکمه‌های تعاملی از نشان‌های اطلاعاتی (Badges vs Buttons Affordance):**
     - *بج‌ها و پیل‌های وضعیت (Status Pills & Counter Badges):* صرفاً برای اعلام وضعیت، سنجه‌های مقداری و تله‌متری هستند و دارای استایل `cursor-default select-none pointer-events-none`، مرزهای ظریف و فاقد فیدبک کلیک می‌باشند تا کاربر موبایل هرگز آن‌ها را با دکمه اشتباه نگیرد.
     - *دکمه‌های اقدام و تعامل (Action Buttons):* دارای هویت بصری فعال، مرز تعاملی، فیدبک هاور/اکتیو و رینگ فوکوس تاکتیکی هستند.
   - **پویایی و تغییر استیت زنده نشانگر زنجیره (Dynamic Streak Vitality Badge):** نشانگر زنجیره با تغییر ثبت ۵ رکن فونداسیون بلافاصله از حالت آرام «حفظ زنجیره با ۵ پایه» به حالت برافروخته رز «زنجیره حفظ شد» تغییر استیت داده و همبستگی پاداش و فیدبک بصری را آشکار می‌سازد.
   - **انطباق ۱۰۰٪ با کاتالوگ کپی‌رایتینگ استویک بوشیدو:** کاربرد متون دقیقاً بر پایه واژگان اصیل و مصوب بوشیدو («کمال تعهد (۱۰ از ۱۰)»، «روز استاندارد (۸ از ۱۰)»، «توقف اضطراری (فریز)»، «پرونده مختومه (کالبدشکافی‌شده)»، «در جریان اجرای روز»، «نیازمند کالبدشکافی»، «حفظ زنجیره با ۵ پایه»، «زنجیره حفظ شد») اعمال می‌گردد و از هرگونه متن‌پردازی سلیقه‌ای یا تفننی پرهیز می‌شود.

---

## ۱۸. چک‌لیست بازبینی طراحی قبل از تحویل (Design QA Checklist)

پیش از نهایی‌سازی و ادغام هر قطعه کد، عامل توسعه باید این ۱۱ بند را چک کند:
- [ ] **۱. انضباط توکن‌ها:** آیا تمامی رنگ‌ها از توکن‌های سمانتیک مصرف شده‌اند و هیچ کد هگز خام درون‌خطی وجود ندارد؟
- [ ] **۲. پرهیز از پالت خام تیل‌ویند:** آیا کلاس‌های رنگی خام مانند `bg-red-500` با توکن‌های سمانتیک جایگزین شده‌اند؟
- [ ] **۳. انضباط رادیوس و اسپیسینگ:** آیا تمام فاصله‌ها بر پایه مضارب ۴px و تمام انحناها بر پایه مقیاس ۷ مرحله‌ای هستند؟
- [ ] **۴. تطابق RTL و ارقام فارسی:** آیا جهت چیدمان راست‌به‌چپ رعایت شده و تمامی اعداد از `toPersianDigits` عبور کرده‌اند؟
- [ ] **۵. کنتراست APCA:** آیا نسبت کنتراست متون به ویژه در حالت شب از حداقل‌های $L_c \ge 78$ عبور می‌کند؟
- [ ] **۶. وضوح فوکوس:** آیا حلقه فوکوس کیبورد (حداقل ۲px) برای تمام کنترل‌ها فعال است؟
- [ ] **۷. مدل وضعیت:** آیا ترتیب اولویت وضعیت‌ها (`Disabled > Loading > Active > Focus > Hover > Default`) رعایت شده است؟
- [ ] **۸. قراردادهای کامپوننت:** آیا ساختار کارت‌های عادات و واریانت‌های دکمه‌ها دقیقاً مطابق قرارداد است؟
- [ ] **۹. انضباط موشن:** آیا تایمینگ‌ها در محدوده ۱۰۰ تا ۳۰۰ میلی‌ثانیه بوده و به `prefers-reduced-motion` احترام گذاشته شده است؟
- [ ] **۱۰. اصل ضد رنگین‌کمان:** آیا کارت‌های عادات در حالت پیش‌فرض از پالت‌های چندرنگ نامرتبط پاک هستند؟
- [ ] **۱۱. انضباط تایپوگرافی:** آیا فونت مونو منحصراً برای اعداد تله‌متری به کار رفته و عناوین با وزیرمتن هماهنگ هستند؟

---

## ۱۹. اصول ۱۵ گانه غیرقابل مذاکره (The 15 Non-Negotiables)

1. **هویت محصول:** سیستم‌عامل انضباط شخصی رزمی، نه اپلیکیشن فانتزی یا اسباب‌بازی عادات.
2. **سلسله‌مراتب توکن‌ها:** حرکت سخت‌گیرانه از توکن پایه به سمانتیک و کامپوننت؛ دور زدن ممنوع.
3. **پیروی مطلق هوش مصنوعی از حاکمیت:** عامل‌های هوش مصنوعی ملزم به رعایت کامل مرزهای مجاز و ممنوع هستند.
4. **اصل ضد انحراف (Anti-Drift):** تغییرات چندکامپوننتی فقط از طریق تغییر توکن در `tokens.css`.
5. **مالکیت انحصاری رنگ‌های معنایی:** هر رنگ اشباع دارای یک نقش تک‌منظوره در کل سامانه است.
6. **سه‌گانه مقدس انضباط (Discipline Holy Trinity):** در تمامی صفحات، آیکون و رنگ این سه شاخص ثابت است:
   - زنجیره مداوم (Streak): `Flame` + `text-rose-400` (`#FB7185`).
   - روزهای استاندارد (Standard Days): `CheckCircle2` + `text-emerald-400` (`#34D399`).
   - امتیاز کل (Total Score): `Award` + `text-amber-400` (`#FBBF24`).
7. **تک‌مرجع هویت برند:** سیستم ۱۰۰٪ بر پایه سرخ اصیل بوشیدو (`#E11D48`) قفل است.
8. **نورپردازی مبتنی بر درخشندگی:** عمق لایه‌ها با $Z_0 \to Z_3$ تامین می‌شود؛ جلوه‌های نئونی ممنوع است.
9. **مرزهای تک‌پیکسلی ضد وایرفریمینگ:** استفاده از بردرهای استاندارد `#27272A` بدون کادرهای سفید تند.
10. **معماری تایپوگرافی دوگانه:** وزیرمتن برای متن‌های انسانی و جت‌برینز مونو برای سنجش و تله‌متری.
11. **بومی‌سازی فارسی و ارقام:** تبدیل ۱۰۰٪ اعداد با `toPersianDigits`.
12. **سکوت در ناوبری:** تغییر روز، ورق زدن تقویم و اسکرول کاملاً بی‌صداست.
13. **انعکاس مستقیم وضعیت روز:** بازتاب امتیاز درون کادر نبرد؛ حذف کامل توست‌های مزاحم روزانه.
14. **هندسه و فاصله‌گذاری گسسته:** شبکه پایه ۴px و مقیاس انحنای ۷ مرحله‌ای.
15. **دسترسی‌پذیری لمسی و فوکوس:** حداقل ابعاد لمسی ۴۴px و حلقه فوکوس مشخص ۲px.

---

## ۲۰. قوانین مهاجرت، پیاده‌سازی آبشاری و توسعه‌های آینده (Migration Rules & Token Cascade)

هنگام دریافت هر دستور تغییر یا افزودن قابلیت جدید، این قوانین حاکم هستند:

### ۱. قانون آبشاری توکن‌ها (The Cascade Rule):
تغییر مقدار یک متغیر CSS در `tokens.css` بلافاصله و به صورت آبشاری در تمامی مصرف‌کنندگان کلاس‌های سمانتیک در کل سامانه اعمال می‌شود. کلیه کدهای جدید و ریفکتورها ملزم به استفاده از متغیرها و کلاس‌های سمانتیک هستند و استفاده از پالت‌های خام یا هگزهای درون‌خطی در کارهای جدید ممنوع است.

> **مثال عملی آبشار:**
> با تغییر `--color-accent-emerald: #34D399` در `tokens.css`، تمامی کلاس‌های `.text-emerald`، `.bg-emerald`، `.bg-emerald-subtle`، `.border-emerald` و شاخص‌های موفقیت روزانه بدون نیاز به لمس فایل‌های JSX به‌روزرسانی می‌شوند.

### ۲. ترتیب فازهای مهاجرت سیستم (Migration Phase Order):
- **فاز ۱ (Shell & Overlays):** پوسته اصلی، نوار بالایی هاب (Navbar / Top Hub)، کنترل‌های سراسری و پنجره‌های شناور (Modals).
- **فاز ۲ (Core Tactical Loop):** نمای میدان نبرد (BattlefieldView)، کارت‌های ۵ عادت، چک‌باکس‌ها و ماتریس تاکتیکی ۹۰ روزه (Tactical Heatmap 90).
- **فاز ۳ (Secondary Dashboards & Admin):** داشبورد تحلیل چرخه (CycleDashboardView)، تالار بایگانی‌ها (ArchivesView)، پروفایل و باقی‌مانده‌های بخش مدیریت.

### ۳. وضعیت کلاس‌های موروثی (Legacy Coexistence & Zero-Glow Expansion):
- کلاس‌های موروثی سازگار (نظیر `.bg-card-obsidian`، `.bg-inner-obsidian`، `.bg-floating-obsidian` و `.theme-glow`) تا زمان رسیدن به نوبت هر ماژول در فازهای مهاجرت معتبر باقی می‌مانند تا از شکست برنامه جلوگیری شود.
- **ممنوعیت گسترش گلو:** هیچ کامپوننت یا نمای جدیدی نباید از کلاس‌های درخشش نئونی (`theme-glow` و غیره) استفاده کند.

### ۴. ممنوعیت تغییر موضعی هگز و عدم ترکیب با ریفکتورهای حجیم:
- اگر نیاز به تنظیم رنگ در صفحه‌ای باشد، این تغییر باید از طریق توکن معنایی مربوطه در `tokens.css` صورت گیرد، نه با وارد کردن هگز جدید و مجزا در کامپوننت.
- تغییرات توکن نباید با ریفکتورهای معماری سنگین (مانند خرد کردن کل فایل‌های اصلی) در یک پرامپت ترکیب شوند تا ریسک خطاهای ناخواسته به صفر برسد.

---

## ۲۱. جدول مرجع توکن‌های CSS و معادل‌های تیل‌ویند (Token Name & CSS Reference Table)

| دسته‌بندی | نام متغیر CSS (`tokens.css`) | مقدار هگز / فرمت | کلاس ابزاری سمانتیک | کلاس معادل Tailwind |
| :--- | :--- | :--- | :--- | :--- |
| **بستر Z0** | `--color-canvas-root` | `#09090b` | `.surface-z0` | `bg-[#09090b]` / `bg-zinc-950` |
| **کارت Z1** | `--color-card-elevated` | `#121215` | `.surface-z1` / `.bg-card-obsidian` | `bg-[#121215]` |
| **کارت Z1 (الیاس)** | `--color-card-elevated-alt` | `#131215` | — | `bg-[#131215]` |
| **محفظه Z2** | `--color-card-inner` | `#18181b` | `.surface-z2` / `.bg-inner-obsidian` | `bg-[#18181b]` / `bg-zinc-900` |
| **محفظه Z2 (الیاس)** | `--color-card-inner-nested` | `#0c0b0e` | — | `bg-[#0c0b0e]` |
| **مودال Z3** | `--color-card-floating` | `#1c1c21` | `.surface-z3` / `.bg-floating-obsidian` | `bg-[#1c1c21]` |
| **کادر تک‌لایه** | `--color-border-subtle` | `#27272a` | `.border-standard` | `border-zinc-800` |
| **کادر هاور** | `--color-border-hover` | `#3f3f46` | `.border-hover` | `border-zinc-700` |
| **کادر فعال** | `--color-border-active` | `#52525b` | `.border-active` | `border-zinc-600` |
| **برند بوشیدو** | `--color-accent-primary` | `#E11D48` | `.text-crimson` / `.theme-bg` | `text-rose-500` / `bg-[#E11D48]` |
| **طلای کهربایی** | `--color-accent-amber` | `#FBBF24` | `.text-amber` / `.bg-amber-subtle` | `text-amber-400` / `bg-amber-500/10` |
| **سبز زمردین** | `--color-accent-emerald` | `#34D399` | `.text-emerald` / `.bg-emerald-subtle` | `text-emerald-400` / `bg-emerald-500/10` |
| **سرخ آتشین** | `--color-accent-rose` | `#FB7185` | `.text-rose` / `.bg-rose-subtle` | `text-rose-400` / `bg-rose-500/10` |
| **سرخ اخطار / بدهی** | `--color-accent-red` | `#F87171` | `.text-debt` / `.bg-debt-subtle` | `text-red-400` / `bg-red-950/30` |
| **سرخ اخطار عمیق** | `--color-accent-red-deep` | `#EF4444` | — | `text-red-500` / `bg-red-600` |
| **آبی فریز** | `--color-accent-blue` | `#60A5FA` | `.text-blue` / `.bg-blue-subtle` | `text-blue-400` / `bg-blue-500/10` |
| **بنفش تحلیل** | `--color-accent-purple` | `#C084FC` | `.text-purple` / `.bg-purple-subtle` | `text-purple-400` / `bg-purple-500/10` |
| **متن اصلی** | `--color-text-primary` | `#F4F4F5` | `.text-role-primary` | `text-zinc-100` / `text-white` |
| **متن ثانویه** | `--color-text-secondary` | `#A1A1AA` | `.text-role-secondary` | `text-zinc-400` |
| **متن فرعی** | `--color-text-muted` | `#71717A` | `.text-role-muted` | `text-zinc-500` |
| **انحنای میکرو** | `--radius-micro` | `4px` | `.radius-micro` | `rounded` |
| **انحنای بج** | `--radius-badge` | `6px` | `.radius-badge` | `rounded-md` |
| **انحنای کنترل** | `--radius-control` | `8px` | `.radius-control` | `rounded-lg` |
| **انحنای کامپوننت** | `--radius-component` | `12px` | `.radius-component` | `rounded-xl` |
| **انحنای کارت** | `--radius-card` | `14px` | `.radius-card` | `rounded-[14px]` |
| **انحنای مودال** | `--radius-modal` | `20px` | `.radius-modal` | `rounded-[20px]` |
| **انحنای کپسولی** | `--radius-capsule` | `9999px` | `.radius-capsule` | `rounded-full` |
| **موشن آنی** | `--duration-instant` | `100ms` | `.motion-instant` | `duration-100` |
| **موشن سریع** | `--duration-fast` | `150ms` | `.motion-fast` | `duration-150` |
| **موشن استاندارد** | `--duration-regular` | `200ms` | `.motion-regular` | `duration-200` |
| **موشن آرام** | `--duration-deliberate` | `300ms` | `.motion-deliberate` | `duration-300` |

---

## ۲۲. حاکمیت موجودیت‌های سمانتیک، سطوح عمق Z در کارت‌های عادات و کادر یادداشت (Semantic Entity Governance & Z-Surface Hierarchy)

برای تضمین این که کاربر موبایل بدون هیچ‌گونه بار شناختی ماهیت هر المان را درک کند و سیستم بدون نیاز به دستکاری‌های مکرر و پراکنده به صورت یکپارچه از یک منبع واحد مدیریت شود:

1. **تفکیک هندسی و نوری کارت‌های عادات از کادر یادداشت (Z-Level Optical Differentiation):**
   - **کارت‌های عادات ($Z_1$ - `.entity-card-habit`):** کارت‌های ۵ رکن فونداسیون و ماموریت ویژه، موجودیت‌های اقدام‌محور اصلی هستند که بر روی بستر خام نبرد ($Z_0$) قرار دارند و از سطح برجسته $Z_1$ (`#121215`) با مرز ۱ پیکسلی استاندارد استفاده می‌کنند. این تفکیک مانع از یک‌دست شدن صفحه و افت پویایی بصری می‌شود.
   - **استامپ دایره‌ای تیک درون عادت ($Z_2 \to \text{Accent}$ - `.entity-stamp-target`):** در حالت انجام‌نشده، درون کارت از سطح $Z_2$ (`#18181b`) استفاده می‌کند تا یک حفره یا فرورفتگی تاکتیکی لمسی (Tactile Inset Well) برای ترغیب انگشت شست کاربر موبایل به تپ ایجاد کند. در صورت انجام شدن، با رنگ سمانتیک سبز زمردین یا طلای کهربایی برافروخته می‌شود.
   - **کادر یادداشت و مشاهدات نبرد ($Z_2$ - `.entity-input-well`):** به عنوان یک فیلد ورودی و دریافت متن (Form Input Well)، درون کانتینر اصلی روی سطح فرورفته $Z_2$ قرار می‌گیرد تا کاربر به وضوح حس یک بستر تایپ‌پذیر و پذیرای یادداشت را بدون اشتباه گرفتن آن با دکمه‌های فشردنی درک کند.

2. **کلاس‌های موجودیت سمانتیک در `tokens.css` (The Master Semantic Entities):**
   - `.entity-hero-panel`: کانتینرهای هدر، خلاصه و پنل‌های سطح اول ($Z_1$, `radius-modal`, `shadow-subtle`).
   - `.entity-card-habit`: کارت‌های عادات تعاملی؛ تنها کنترل تعاملی سطر در قالب دکمه تمام‌عرض (`w-full min-h-[44px]`) منطبق بر Apple HIG (~44pt) و WCAG 2.5.5 / WCAG 2.5.8، استیت‌های `is-checked-standard` و `is-checked-mastery` و فیدبک `active:scale-[0.99]`.
   - `.entity-stamp-target`: نشانگر وضعیت و بازخورد بصری استامپ تیک (صرفاً شاخص بصری، نه کنترل مجزا؛ عدم الزام به ابعاد ۴۴×۴۴) با گردی کپسولی کامل و ترنزیشن رنگی دقیق.
   - `.entity-input-well`: کادرهای ورودی و تکست‌آریا با استیت‌های فوکوس تاکتیکی و هویت بدون لرزش.
   - `.entity-metric-card` و `.entity-metric-card-nested`: کارت‌های بنتو گرید تله‌متری و رکوردها در اتاق فرمان و داشبوردها.
   - `.entity-status-badge`: نشان‌ها و پیل‌های وضعیت سمانتیک (ارتفاع ثابت ۲۶ پیکسل، `radius-control` ۸ پیکسل، کاملاً غیرقابل کلیک `cursor-default select-none pointer-events-none`).
   - `.entity-telemetry-badge`: نشانگرهای سنجش کمی نظیر «X از ۵ پایه» با پس‌زمینه خنثی و تراز نوری دقیق با پیل‌های وضعیت همجوار.
   - `.entity-gauge-track`: شیار مهندسی‌شده کپسولی برای گیج‌های ده تکه پیشرفت و امتیاز روزانه.
   - `.entity-action-btn`: دکمه‌های تعاملی با هدف لمسی استاندارد (حداقل ۴۴ پیکسل برای موبایل)، افوردانس لمسی و فیدبک فشرده‌سازی (`active:scale-[0.98]`).

3. **ارگونومی نوار ناوبری تاریخ و کات‌آف شبانه (Temporal Navigator & Cutoff Symmetry):**
   - **مرکزیت کات‌آف شبانه:** نشانگر کات‌آف در ساب‌بار اختصاصی زیر تاریخ و به صورت کاملاً متقارن در مرکز کادر قرار می‌گیرد (`justify-center`).
   - **قاعده منع تکرار تاریخ (Zero Date Redundancy Rule):** از آنجا که تاریخ روز جاری در هدر نوار ناوبری با فونت درشت و خوانا درج شده است، در بنرهای زمینه روزهای گذشته یا آینده نباید تاریخ شمسی مجدداً در قالب پیل تکرار شود؛ صرفاً برچسب‌های زمانی معنایی («X روز بعد» یا «مشاهده تاریخچه») کفایت می‌کند.

---

## ۲۳. کالبدشناسی و نقشه مهندسی ۳ کادر فوقانی میدان نبرد (Anatomy & Engineering Specification of Top 3 Battlefield Master Panels)

تمامی المان‌ها، کانتینرها، دکمه‌ها، نشانگرهای تله‌متری و متون در سه پنل فوقانی میدان نبرد به صورت میکروسکوپیک با تفکیک کامل لایه‌های والد-فرزند (`div` / `span` / `button`)، شناسه‌های یکتا (`id`) و توکن‌های سیستم طراحی مهندسی شده‌اند:

### ۱. پنل پیش‌نمایش داده‌های نمونه (`#battlefield-demo-banner`):
* **کانتینر اصلی:** `id="battlefield-demo-banner"`, کلاس `surface-z1 border-standard radius-card p-3.5 sm:p-4 text-xs shadow-subtle flex flex-col sm:flex-row items-start sm:items-center justify-between gap-3 sm:gap-4 animate-in fade-in slide-in-from-top-2 relative`
* **خوشه محتوا:** `id="battlefield-demo-content-cluster"`, کلاس `flex items-start gap-3 min-w-0 flex-1 w-full sm:w-auto`
  * **محفظه آیکون جرقه:** `id="battlefield-demo-icon-container"`, کلاس `w-8 h-8 radius-control bg-amber-subtle border border-amber-subtle text-amber flex items-center justify-center shrink-0`
    * **پوشش آیکون:** `id="battlefield-demo-icon-wrap"`, کلاس `inline-flex items-center justify-center` حاوی `<Sparkles className="w-4 h-4 text-amber" />`
  * **ستون متن و متادیتا:** `id="battlefield-demo-text-col"`, کلاس `min-w-0 flex-1 space-y-1`
    * **ردیف هدر:** `id="battlefield-demo-header-row"`, کلاس `flex items-center justify-between gap-2`
      * **خوشه عنوان و بج:** `id="battlefield-demo-title-badge-cluster"`, کلاس `flex items-center gap-2 flex-wrap`
        * **عنوان:** `id="battlefield-demo-title"`, کلاس `font-bold text-role-primary text-xs sm:text-sm` حاوی `id="battlefield-demo-title-text"`
        * **بج نمونه:** `id="battlefield-demo-badge"`, کلاس `text-[10px] bg-amber-subtle text-amber border border-amber-subtle px-2 py-0.5 radius-micro font-mono font-bold select-none pointer-events-none inline-flex items-center` حاوی `id="battlefield-demo-badge-text"` («۲۴ روز نمونه»)
      * **دکمه ضربدر بستن در موبایل:** `id="battlefield-demo-dismiss-mobile"`, دکمه `sm:hidden text-role-secondary hover:text-role-primary p-1.5 radius-control border-standard surface-z1 hover:surface-z2 cursor-pointer shrink-0 inline-flex items-center justify-center` حاوی `id="battlefield-demo-dismiss-mobile-icon-wrap"` با `<X className="w-4 h-4" />`
    * **کانتینر روایت سناریو:** `id="battlefield-demo-narrative-container"` حاوی `id="battlefield-demo-narrative-text"` (`text-[11px] text-role-secondary leading-relaxed`)
* **خوشه اقدامات و اکشن‌ها:** `id="battlefield-demo-actions-cluster"`, کلاس `flex items-center gap-2 w-full sm:w-auto shrink-0 justify-end pt-2 sm:pt-0 border-t sm:border-t-0 border-[var(--color-border-subtle)]`
  * **دکمه اقدام شروع چرخه واقعی:** `id="battlefield-demo-create-cycle-btn"`, دکمه `w-full sm:w-auto bg-amber hover:brightness-110 text-[var(--color-canvas-root)] font-black text-xs px-4 py-2 radius-control cursor-pointer active:scale-95 shadow-subtle inline-flex items-center justify-center gap-1.5` حاوی `id="battlefield-demo-create-cycle-icon-wrap"` (`<Sparkles className="w-3.5 h-3.5" />`) و `id="battlefield-demo-create-cycle-text"` («شروع چرخه واقعی»)
  * **دکمه ضربدر بستن در دسکتاپ:** `id="battlefield-demo-dismiss-desktop"`, دکمه `hidden sm:inline-flex text-role-secondary hover:text-role-primary p-2 radius-control border-standard surface-z1 hover:surface-z2 cursor-pointer items-center justify-center` حاوی `id="battlefield-demo-dismiss-desktop-icon-wrap"` با `<X className="w-3.5 h-3.5" />`

### ۲. پنل ناوبری تاریخ و کات‌آف شبانه (`#battlefield-date-navigator`):
* **کانتینر اصلی:** `id="battlefield-date-navigator"`, کلاس `surface-z1 border-standard radius-card p-3 sm:p-4 shadow-subtle select-none space-y-2.5 sm:space-y-3`
* **ردیف اصلی ناوبری:** `id="battlefield-date-nav-main-row"`, کلاس `flex items-center justify-between gap-2 sm:gap-4 w-full`
  * **دکمه روز قبل:** `id="battlefield-prev-day-btn"`, دکمه `h-9 sm:h-10 px-2.5 sm:px-3.5 surface-z2 hover:surface-z3 text-role-primary radius-component cursor-pointer border-standard shadow-subtle active:scale-95 inline-flex items-center justify-center gap-1.5 text-xs font-bold whitespace-nowrap shrink-0`
    * **پوشش آیکون:** `id="battlefield-prev-day-icon-wrap"`, حاوی `<ChevronRight className="w-4 h-4 text-role-muted" />`
    * **متن دکمه:** `id="battlefield-prev-day-label"`, کلاس `hidden sm:inline whitespace-nowrap leading-none` («روز قبل»)
  * **ستون مرکزی نمایش تاریخ:** `id="battlefield-date-center-col"`, کلاس `flex-1 min-w-0 text-center px-1 flex flex-col items-center justify-center space-y-0.5`
    * **کپسول برچسب نسبی روز:** `id="battlefield-relative-date-badge"`, کلاس `text-[11px] sm:text-xs text-role-secondary font-semibold inline-flex items-center justify-center`
      * **متن برچسب نسبی:** `id="battlefield-relative-date-label"`, کلاس `whitespace-nowrap` («روز جاری نبرد»، «X روز بعد»، «X روز قبل»)
    * **کانتینر سرتیتر تاریخ:** `id="battlefield-date-heading-container"`, کلاس `inline-flex items-center justify-center`
      * **سرتیتر تاریخ رسمی شمسی:** `id="battlefield-date-heading"`, عنوان `text-xs sm:text-sm md:text-base font-black text-role-primary tracking-tight font-mono whitespace-nowrap leading-none`
  * **دکمه روز بعد:** `id="battlefield-next-day-btn"`, دکمه `h-9 sm:h-10 px-2.5 sm:px-3.5 surface-z2 hover:surface-z3 text-role-primary radius-component cursor-pointer border-standard shadow-subtle active:scale-95 inline-flex items-center justify-center gap-1.5 text-xs font-bold whitespace-nowrap shrink-0`
    * **متن دکمه:** `id="battlefield-next-day-label"`, کلاس `hidden sm:inline whitespace-nowrap leading-none` («روز بعد»)
    * **پوشش آیکون:** `id="battlefield-next-day-icon-wrap"`, حاوی `<ChevronLeft className="w-4 h-4 text-role-muted" />`
* **ساب‌بار کات‌آف شبانه:** `id="battlefield-cutoff-sub-bar"`, کلاس `pt-2 sm:pt-2.5 border-t border-[var(--color-border-subtle)] flex items-center justify-center text-[10px] sm:text-[11px]`
  * **کپسول متقارن و متمرکز کات‌آف:** `id="battlefield-cutoff-badge"`, کلاس `.entity-telemetry-badge inline-flex items-center gap-1.5 font-medium shadow-subtle leading-none`
    * **پوشش آیکون ساعت:** `id="battlefield-cutoff-icon-wrap"`, حاوی `<Clock className="w-3.5 h-3.5 text-role-muted" />`
    * **گروه متنی کات‌آف:** `id="battlefield-cutoff-text-group"`, کلاس `inline-flex items-center gap-1`
      * **برچسب کات‌آف:** `id="battlefield-cutoff-label"`, کلاس `text-role-secondary` («کات‌آف شبانه:»)
      * **مقدار عددی کات‌آف:** `id="battlefield-cutoff-value"`, کلاس `text-role-primary font-mono font-bold` («۰۴:۰۰ بامداد»)

### ۳. پنل جامع وضعیت، تله‌متری و امتیاز روز نبرد (`#battlefield-hero-panel`):
* **کانتینر والد پنل نبرد:** `id="battlefield-hero-panel"`, کلاس `.entity-hero-panel w-full max-w-full p-4 sm:p-5 relative overflow-hidden`
* **چیدمان اصلی فلکس:** `id="battlefield-hero-main-layout"`, کلاس `flex flex-col md:flex-row md:items-center justify-between gap-4 sm:gap-6`
  * **ستون سمت راست (تله‌متری و روایت مربی):** `id="battlefield-hero-narrative-col"`, کلاس `space-y-3 flex-1 min-w-0`
    * **خوشه تله‌متری دوگانه:** `id="battlefield-telemetry-cluster"`, کلاس `flex items-center gap-2 flex-wrap`
      * **نشانگر کمی پایه‌ها:** `id="battlefield-habits-count-badge"`, کلاس `.entity-telemetry-badge font-mono shrink-0 inline-flex items-center gap-1`
        * `id="battlefield-habits-count-current"`: مقدار عدد جاری (`text-role-primary font-bold`)
        * `id="battlefield-habits-count-separator"`: جداکننده «از» (`text-role-muted`)
        * `id="battlefield-habits-count-total"`: کل پایه‌ها «۵» (`text-role-secondary font-bold`)
        * `id="battlefield-habits-count-unit"`: واحد «پایه» (`text-role-muted text-[10px]`)
      * **پیل وضعیت زنجیره:** `id="battlefield-streak-vitality-badge"`, کلاس `.entity-status-badge border inline-flex items-center gap-1.5`
        * `id="battlefield-streak-vitality-icon-wrap"`: پوشش آیکون شعله `<Flame className="w-3.5 h-3.5 shrink-0" />`
        * `id="battlefield-streak-vitality-label"`: برچسب وضعیت زنجیره (`whitespace-nowrap leading-none`)
    * **کانتینر روایت و حکمت مربی:** `id="battlefield-coach-commentary-container"`, کلاس `min-h-[1.5rem] pt-0.5`
      * `id="battlefield-coach-commentary"`: پاراگراف تحلیل مربی دیسیپلین (`text-xs sm:text-sm text-role-secondary font-medium leading-relaxed`)
  * **کارت امتیاز مستقل ($Z_2$ Nested Well):** `id="battlefield-score-card"`, کلاس `.entity-metric-card-nested w-full md:w-[220px] shrink-0 p-3.5 sm:p-4 flex flex-col items-center justify-center gap-2.5 text-center shadow-subtle`
    * **هدر امتیاز:** `id="battlefield-score-header"`, کلاس `text-[11px] sm:text-xs text-role-secondary font-medium flex items-center justify-center gap-1.5 whitespace-nowrap select-none pointer-events-none`
      * `id="battlefield-score-header-label"`: متن «امتیاز ارزش روز»
      * `id="battlefield-score-header-icon-wrap"`: آیکون داینامیک شمشیر یا شیلد
    * **ارقام بزرگ امتیاز روز:** `id="battlefield-score-value"`, کلاس `text-3xl sm:text-4xl font-black font-mono flex items-baseline justify-center gap-1.5`
      * `id="battlefield-score-number"`: عدد امتیاز (`leading-none`)
      * `id="battlefield-score-max"`: کپسول مخرج کسر (`text-xs font-semibold text-role-muted inline-flex items-center gap-0.5`) شامل `id="battlefield-score-max-sep"` («از») و `id="battlefield-score-max-num"` («۱۰»)
    * **ریبون تک‌مرجع وضعیت قطعی روز (Single Source of Truth):** `id="battlefield-score-ribbon"`, کانتینر ثابت ۲۴ پیکسلی حاوی:
      * `id="battlefield-score-status-badge"`: پیل سمانتیک `.entity-status-badge`
        * `id="battlefield-score-status-icon-wrap"`: پوشش آیکون وضعیت (`<Swords />` / `<ShieldCheck />` / `<Clock />` / `<Compass />` / ...)
        * `id="battlefield-score-status-label"`: برچسب وضعیت قطعی روز (`whitespace-nowrap leading-none`)
    * **شیار گیج ۱۰ تکه پیشرفت:** `id="battlefield-score-gauge"`, کلاس `.entity-gauge-track w-full flex items-center gap-1 justify-center select-none pointer-events-none`
      * ۱۰ کپسول مجزا با شناسه‌های یکتای `id="battlefield-gauge-segment-1"` تا `id="battlefield-gauge-segment-10"` (`h-2 flex-1 radius-capsule border transition-colors duration-150`)

---

## ۱۴. استاندارد جامع نشانه‌شناسی و مهار نویز بصری آیکون‌ها (Iconography & Anti-Visual Noise Protocol)

جهت جلوگیری از سلیقه‌ای شدن حضور آیکون‌ها و حفظ وقار استویک سیستم، دستورالعمل قطعی زیر حاکم است:

### الف) کجا آیکون الزامی است؟ (Mandatory Icons)
1. **سرتیتر بخش‌های اصلی ($Z_1$ Section Headings):** منحصراً یک آیکون در سمت راست تیتر اصلی برای جهت‌دهی سریع نگاه (مثل شمشیرها برای ۵ رکن، راکت برای ماموریت ویژه، مغز برای کالبدشکافی). رنگ آیکون سرتیتر بخش‌ها **همواره خنثی زینک** (`text-zinc-200` یا `text-zinc-400`) است.
2. **کارت‌های تعاملی عادات ($Z_2$ Habit Cards):** محفظه آیکون ۴۰ پیکسلی با آیکون مفهومی اختصاصی هر عادت (ورزش، مطالعه، سحرخیزی و...).
3. **ریبون‌های وضعیت سمانتیک داینامیک (Status Badges):** پیل‌های وضعیت روز نبرد (مثل شمشیر کمال ۱۰/۱۰، شیلد روز استاندارد، برفک فریز، هشدار بدهی).
4. **سه‌گانه مقدس دیسیپلین (Holy Trinity Metrics):** زنجیره (`Flame` سرخ)، روزهای استاندارد (`CheckCircle2` زمردی) و امتیاز کل (`Award` کهربایی).
5. **دکمه‌های اقدام کلیدی (Action Buttons):** برای درک سریع ماهیت اقدام (مثل ضربدر برای بستن، آیکون کالبدشکافی برای اقدام، شمشیر برای شروع چرخه).

### ب) کجا آیکون اکیداً ممنوع و پارازیت بصری است؟ (Forbidden & De-cluttered Locations)
1. **بج‌های کمکی و راهنما در کنار سرتیترها (Sub-badges & Helper Requirements):** بج‌های سمت چپ هدرها (مانند «شرط روز استاندارد: ۵ از ۵ پایه» یا «پاداش کمال: +۲ امتیاز») باید تایپوگرافی خالص با ارقام مونو باشند و هیچ آیکونی در آنها قرار نمی‌گیرد.
2. **برچسب‌های تقویم و زمان نسبی (Relative Date & Timeline Badges):** در نوار ناوبری تاریخ که عنوان بزرگ تاریخ شمسی درج شده، استفاده از آیکون تقویم در کنار برچسب روز نسبی («روز جاری نبرد»، «۲ روز قبل») حشو است و حذف می‌شود.
3. **تکرار بج امتیاز در داخل دکمه‌های اقدام (No Redundant Score Pills inside Buttons):** هنگامی که در هدر بخش میزان پاداش قید شده است، تکرار چیپ `+۲ امتیاز` در متن خود دکمه ممنوع است و عنوان دکمه به صورت موجز و رسا (`ماموریت ویژه روز`) نگارش می‌شود.
4. **کارت‌ها و محفظه‌های نمایشی استاتیک (Non-Interactive Metric Wells):** کادرهای آماری ثابت نباید هیچ افکت هاور تعاملی یا آیکون‌های فانتزی تصادفی داشته باشند.

---

## ۱۵. استاندارد کارت شاخص انضباط دوره و همتایی با کارت روزانه نبرد (Cycle Discipline Score Twin Card)

کارت شاخص انضباط کل دوره در اتاق فرماندهی (`#cycle-discipline-score-card`) دارای همتایی هندسی، بصری و ساختاری ۱۰۰٪ با کارت امتیاز روزانه در میدان نبرد است:

* **شناسه کانتینر اصلی:** `id="cycle-discipline-score-card"`, کلاس `.entity-metric-card-nested p-3.5 sm:p-4 text-center flex flex-col items-center justify-center gap-2.5 shadow-subtle w-full max-w-[260px] mx-auto lg:max-w-none lg:w-full aspect-[1.618/1] sm:aspect-auto select-none pointer-events-none`
* **هدر شاخص دوره:** `id="cycle-discipline-score-header"`, کلاس `text-[11px] sm:text-xs text-role-secondary font-medium flex items-center justify-center gap-1.5 whitespace-nowrap`
  * `id="cycle-discipline-score-header-label"`: متن «شاخص انضباط دوره»
  * `id="cycle-discipline-score-header-icon-wrap"`: حاوی `<TrendingUp className="w-3.5 h-3.5 text-role-muted" />`
* **ارقام بزرگ درصد انضباط:** `id="cycle-discipline-score-value"`, کلاس `text-3xl sm:text-4xl font-black font-mono flex items-baseline justify-center gap-1 text-role-primary`
  * `id="cycle-discipline-score-number"`: رقم شاخص (مانند «۸۶») با تایپوگرافی مونو
  * `id="cycle-discipline-score-unit"`: علامت درصد («٪») در `text-xs font-semibold text-role-muted`
* **ریبون وضعیت دیسیپلین دوره:** `id="cycle-discipline-score-ribbon"`, کانتینر ۲۴ پیکسلی یکپارچه
  * `id="cycle-discipline-score-status-badge"`: پیل سمانتیک وضعیت (زمردی برای $\ge 80\%$, کهربایی برای ۷۰ تا ۷۹٪، قرمز برای $< 70\%$، خنثی در انتظار)
  * `id="cycle-discipline-score-status-icon-wrap"`: پوشش آیکون سمانتیک
  * `id="cycle-discipline-score-status-label"`: برچسب وضعیت
* **پاورقی متدولوژی:** `id="cycle-discipline-score-caption"`, کلاس `text-[10px] text-role-muted text-center leading-normal` («محاسبه پیوسته با مخرج شبح طبق متدولوژی بوشیدو»)

---

## ۱۶. استاندارد ماتریس تاکتیکی ۹۰ روزه و پالت کنتراست تیره (Tactical 90-Day Matrix & Dark Contrast Protocol)

ماتریس ۹۰ روزه (`TacticalHeatmap90.tsx`) نمای جامع و تعاملی کل چرخه را با پالت کنتراست تیره، هماهنگ با استانداردهای APCA و بدون ناهماهنگی بصری فراهم می‌کند:

* **شناسه کانتینر:** `id="tactical-heatmap-container"`, کلاس `.entity-hero-panel w-full max-w-full p-3.5 sm:p-5 md:p-7 space-y-4 sm:space-y-5 overflow-hidden`
* **سرتیتر و نشانه‌ها:**
  * `id="tactical-heatmap-heading"`: عنوان «ماتریس تاکتیکی ۹۰ روزه» (بدون پرانتز انگلیسی)
  * `id="tactical-heatmap-elapsed-badge"`: بج «روز X از ۹۰» (تایپوگرافی خالص مونو بدون آیکون)
  * `id="tactical-heatmap-legend"`: راهنمای رنگ‌های سمانتیک بر پایه پس‌زمینه‌های تیره ساب‌تل (`bg-*-subtle`) و فونت‌های درخشان سمانتیک (`text-amber`, `text-emerald`, `text-blue`, `text-debt`, `text-purple`).
* **سلول روز جاری (Today's Cell):** با بوردر ۲ پیکسلی کهربایی مستقیم (`border-2 border-amber ring-1 ring-amber/30 scale-105 z-10`) بدون گپ‌های سیاه اضافی (`ring-offset`) برای دقت و وضوح حداکثری.
* **شبکه ۹۰ سلولی:** `id="tactical-heatmap-grid"`, ابعاد سلول‌ها با حداقل ارتفاع ۴۴ پیکسل برای تاچ استاندارد موبایل (`min-h-[44px]`), تمام اعداد با ارقام فارسی مونو و کنتراست بالای Lc > 75 در دارک‌مود.
---

## ۱۷. استاندارد ماتریس وفاداری به ارکان پنج‌گانه (Habit Fidelity Matrix Protocol)

ماتریس وفاداری (`HabitFidelityMatrix.tsx`) عملکرد تفکیکی ۵ رکن شکست‌ناپذیر و ماموریت ویژه را محاسبه و عرضه می‌کند:

* **شناسه کانتینر:** `id="fidelity-matrix-container"`, کلاس `.entity-hero-panel p-5 sm:p-7 space-y-6`
* **هدر و نشانگر تجمیعی:**
  * `id="fidelity-matrix-heading"`: عنوان «ماتریس وفاداری به ارکان دیسیپلین»
  * `id="fidelity-matrix-active-days-badge"`: برچسب مونو «ارزیابی X روز فعال» (بدون آیکون)
  * `id="fidelity-matrix-aggregate-card"`: کادر تجمیعی وفاداری با شیلد زمردی (`ShieldCheck`) و میانگین درصدی
* **کارت‌های ۶ گانه:** `id="fidelity-card-habit-wakeEarly"`, `id="fidelity-card-habit-exercise"`, `id="fidelity-card-habit-deepWork"`, `id="fidelity-card-habit-study"`, `id="fidelity-card-habit-dailyReview"`, `id="fidelity-card-special-mission"` با استایل `.entity-metric-card-nested`, نوارهای پیشرفت روان با `transition-[width] duration-500`، و بج‌های سطح‌بندی پایدار.

---

## ۱۸. پروتکل پالایش ایموجی‌ها و انحصار آیکون‌های برداری سمانتیک (Text Emoji Ban & Semantic SVG Exclusivity)

برای حفظ وقار استویک و یکنواختی نمایش در تمامی سیستم‌عامل‌ها (iOS, Android, Windows, macOS):

۱. **ممنوعیت درج ایموجی در رشته‌های داده‌ای (No Emojis in Data Strings):** تمام رشته‌های خروجی موتور محاسبات و تعاریف داده (مانند `statusLabelFa` و `disciplineLevel`) باید تایپوگرافی خالص باشند (`چرخه فعال`، `انضباط آهنین`، `انضباط پایدار`، `بحران تعهد`، `آماده برای قرارگاه بوشیدو`) بدون کاراکترهای ایموجی مثل ⚡، 🛡️، 👹، 📦.
۲. **تفکیک مسئولیت بصری (Visual Separation of Concerns):** نمایش نمادهای بصری منحصراً بر عهده کامپوننت‌های برداری سمانتیک (`Lucide React SVG`) است که بر اساس وضعیت داده (`metrics.status`, `metrics.disciplinePercentage`) با توکن‌های دقیق رنگی رندر می‌شوند.
۳. **حذف بج‌های تکراری در تب‌ها (Zero Redundancy in Sub-Tabs):** اگر عنوان تب خود مشخص‌کننده بازه است (مانند «نقشه ۹۰ روزه»)، افزودن بج مکمل با همان عنوان (`۹۰ روز`) حشو و ممنوع است.

---

## ۱۹. سامانه قراردادهای دکمه‌های کنترلی (The Button Contracts System)

برای ایجاد نظم کامل در رفتارهای تعاملی و پرهیز از تداخل رنگ‌های بصری، توکن‌های دکمه‌ها در قراردادهای ساختاریافته در `tokens.css` تعریف شده‌اند:

1. **`btn-contract-primary` (اقدامات اصلی و دیسیپلین سرخ):**
   - پس‌زمینه زرشکی دیسیپلین (`--color-accent-primary`), متن سفید، هاور تیره/روشن شونده و مقیاس لمسی ۹۸٪ در حالت فشرده.
   - کاربرد: تعهدات اصلی، ثبت‌های حساس، بستن بدهی‌ها و اقدامات مستقیم دیسیپلین (مانند «تایید خروج از بایگانی»، «ثبت ماموریت»).

2. **`btn-contract-mastery` (کمال ۱۰/۱۰، ارتقای VIP و کمال‌گرایی سامورایی):**
   - پس‌زمینه کهربایی (`--color-accent-amber`), متن تیره کنواس (`#09090b`), فیلتر روشنایی در هاور.
   - کاربرد: اقدامات شتاب‌دهنده، خرید و فعال‌سازی اشتراک VIP، شروع چرخه جدید و تأیید نهایی ارتقا.

3. **`btn-contract-secondary` (اقدامات ثانویه روی سطوح برآمده):**
   - پس‌زمینه سطحی اینر (`--color-card-inner`), بوردر ظریف (`--color-border-subtle`), متن اصلی.
   - کاربرد: دکمه‌های خنثی، ناوبری تاریخ قبل/بعد، فیلترها، تغییر فرم و انصراف‌های مرزی.

4. **`btn-contract-ghost` (اقدامات کم‌تاکید و شفاف):**
   - پس‌زمینه شفاف (`transparent`), بدون بوردر پیش‌فرض، تغییر پس‌زمینه در هاور به کارت داخلی.
   - کاربرد: دکمه بستن مدال‌ها (X)، انصراف ملایم، اکشن‌های متنی و لینک‌های خنثی.

5. **`btn-contract-danger` (اقدامات مخرب قطعی در مدال‌ها - Destructive Commit):**
   - پس‌زمینه قرمز پررنگ بدهی و اخطار (`--color-accent-red-deep` / `#ef4444`)، متن سفید خالص، هاور تیره‌تر زرشکی (`#dc2626`) با فیدبک لمسی مقیاس ۹۸٪.
   - کاربرد: تأیید نهایی اقدامات غیرقابل‌بازگشت مانند «حذف قطعی چرخه» و «بله، بازنشانی داده‌ها».

6. **`btn-contract-danger-subtle` (تریگر سطحی اقدامات مخرب - Destructive Surface Trigger):**
   - پس‌زمینه خنثی هماهنگ با سطح (`--color-card-inner`) با بوردر ظریف و متن ملایم؛ در حالت هاور بلافاصله به رنگ زرشکی هشدار (`color: var(--color-accent-red)`، `background: var(--color-accent-red-bg)` و `border: var(--color-accent-red-border)`) تبدیل می‌شود و فیدبک واضح بصری به کاربر ارائه می‌دهد.
   - کاربرد: دکمه‌های تریگر حذف روی نوارهای ابزار و کارت‌ها (مانند دکمه «حذف چرخه» در نوار بایگانی).

7. **`btn-contract-danger-ghost` (تریگر شفاف اقدامات مخرب - Destructive Ghost Trigger):**
   - پس‌زمینه شفاف در حالت سکون؛ در هاور به پس‌زمینه قرمز ملایم و متن قرمز اخطار تغییر می‌کند.
   - کاربرد: دکمه‌های متنی در ردیف جداول مدیریت و دراپ‌داون‌ها (مانند «تنزل کاربر» یا آیکون حذف در منوی چرخه).

### ۸. قانون کمیابی دکمه اصلی و تفکیک خطر از رنگ برند (Primary Scarcity & Danger Separation Rule)
- **کمیابی دکمه اصلی (Primary Scarcity):** در هر ویو یا مودال حداکثر **یک** دکمه با قرارداد `btn-contract-primary` (سرخ توپر برند) مجاز است. اکشن‌های ناوبری، پرش تاریخی، جابه‌جایی صفحات و اکشن‌های ثانویه باید اکیداً از `btn-contract-secondary` یا `btn-contract-ghost` استفاده کنند.
- **تفکیک اکشن‌های خطر از رنگ سرخ برند (Danger vs Brand Separation):** تریگرهای پاک‌سازی داده، بازنشانی، خروج یا اقدامات مخرب هرگز نباید از `btn-contract-primary` استفاده کنند؛ این اکشن‌ها باید از `btn-contract-danger-subtle` (برای تریگرهای سطحی) یا `btn-contract-danger` (صرفاً برای تایید نهایی در مودال) بهره ببرند.

---

## ۲۰. قرارداد عمق سطوح و فرورفتگی‌ها (Surface Z & Inset-Well Contract)
- **بستر کنواس (Page Canvas):** ریشه صفحه همواره `surface-z0` / `var(--color-canvas-root)` (`#09090b`) است.
- **کارت‌ها و بخش‌ها (Sections / Cards):** کانتینرهای اصلی محتوا با `surface-z1` + `border-standard` + `radius-card` / `radius-modal` مشخص می‌شوند.
- **سطوح برآمده و چیپ‌ها (Raised Platforms / Sub-containers):** المان‌های داخلی کارت‌ها و چیپ‌های آیکون روی `surface-z2` قرار می‌گیرند.
- **تاکید داخلی و مودال‌های شناور (Modals & Floating Emphasis):** سطوح شناور و تاکید برآمده در مودال‌ها با `surface-z3` رندر می‌شوند.
- **چاه‌های فرورفته (Inset Wells):** اینپوت‌ها، نوارهای پیشرفت و بلوک‌های محتوای فرورفته واقعی منحصراً از `surface-z0` استفاده می‌کنند؛ استفاده تزئینی یا خالی از چاه‌های فرورفته ممنوع است.

---

## ۲۲. سلسله‌مراتب دکمه تسلط در بخش حساب (Single Dominant Mastery CTA Rule)
- **یک اکشن غالب تسلط (Single Dominant Mastery):** در هر لحظه در کارت حساب کاربری صرفاً **یک** دکمه با قرارداد `btn-contract-mastery` مجاز است:
  - در حالت کاربر مهمان (Logged Out): دکمه «ورود یا ایجاد حساب کاربری» نقش تسلط (`btn-contract-mastery`) را بر عهده دارد و ارتقای VIP به اکشن ثانویه (`btn-contract-secondary`) تبدیل می‌شود.
  - در حالت کاربر وارد شده غیر VIP: دکمه «ارتقا به حساب سامورایی ویژه (VIP)» نقش تسلط (`btn-contract-mastery`) است و بقیه اکشن‌ها ثانویه هستند.
  - در حالت کاربر VIP: دکمه «تمدید اشتراک سامورایی ویژه» نقش تسلط (`btn-contract-mastery`) را دارد.

---

## ۲۳. قانون خنثی‌سازی پس‌زمینه کارت‌های متریک (Anti-Rainbow Metric Card Rule)
- **پوسته خنثی کارت (`surface-z1` / `border-standard`):** بدنه کارت‌های متریک در داشبورد فرماندهی، تالار رکوردها و پنل مدیریت همواره دارای پس‌زمینه خنثی زینک (`surface-z1` یا `entity-metric-card`) و بوردر استاندارد است.
- **انحصار رنگ معنایی به مقدار و آیکون (Semantic Accents on Value/Icon Only):** از اعمال رنگ‌آمیزی کل بدنه کارت با رنگ‌های اشباع (Chrome Washes) پرهیز می‌شود. رنگ‌های معنایی تثلیث انضباطی (رز آتشین، زمرد سرزندگی، کهربای تسلط، قرمز بدهی و آبی فریز) صرفاً روی آیکون، برچسب‌ها یا عدد مقدار اصلی اعمال می‌شوند تا کارت‌ها ظاهری متین، منظم و خوانا داشته باشند.

---

## ۲۴. استاندارد وضعیت‌های خالی و فوکوس مسیر روزانه (Daily Path Empty & Focus Contract)
- **الگوی وضعیت خالی (Standardized Empty State):** شامل سه‌گانه وضعیت + توضیح کوتاه + تک‌اکشن بعدی بر روی `surface-z1` با `border-standard` بدون تصویرسازی‌های نامربوط.
- **بهداشت فوکوس تاکتیکی (Tactical Focus Hygiene):** تمامی دکمه‌ها و المان‌های تعاملی اصلی مسیر روزانه از حلقه فوکوس استاندارد `focus-ring-tactical` با کنتراست دقیق و بدون خطوط خشن پیش‌فرض سفید استفاده می‌کنند.
- **تعامل ساب‌تب‌های چسبان (Sticky Sub-Tab Clearance):** هدر چسبان و ساب‌تب‌ها حریم امن `pt-safe` و آفست استاندارد را رعایت کرده و در اسکرول با یکدیگر تداخل ندارند.






